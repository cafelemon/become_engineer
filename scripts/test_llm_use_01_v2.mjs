import assert from"node:assert/strict";import{spawnSync}from"node:child_process";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lesson=readFileSync(resolve(root,"learning-paths/llm-agent/model-use/01-model-boundary-messages-offline-adapter.md"),"utf8");for(const type of["overview","concept","example","reproduce","modify","troubleshoot","project"])assert.match(lesson,new RegExp(`data-context-type="${type}"`));
for(const phrase of["completed=status:completed","refused=status:refused","incomplete=status:incomplete","empty_completed=rejected","normalized_result=status+text+usage+finish_reason+request_id","model-is-untrusted-adapter,application-validates,offline-first,no-rag,no-tools"])assert.ok(lesson.includes(phrase));for(const phrase of["零基础兴趣","有基础求职","Protocol","招聘信号","Python 3.11+"])assert.ok(lesson.includes(phrase));
const tests=spawnSync("python3",["-m","unittest","-v","test_model_boundary.py"],{cwd:resolve(root,"site-src/examples/llm-use/intelligent-learning-assistant-v01"),encoding:"utf8"});assert.equal(tests.status,0,tests.stdout+tests.stderr);assert.match(tests.stdout+tests.stderr,/Ran 8 tests/);console.log(JSON.stringify({valid:true,lesson_id:"llm-use-01",tests:8,offline:true,normalized_statuses:3},null,2));
