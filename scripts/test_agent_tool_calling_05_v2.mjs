import assert from"node:assert/strict";import{spawnSync}from"node:child_process";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lesson=readFileSync(resolve(root,"learning-paths/llm-agent/tool-calling-workflow/05-bounded-tool-loop-state-budgets-termination.md"),"utf8");for(const type of["overview","concept","example","reproduce","modify","troubleshoot","project"])assert.match(lesson,new RegExp(`data-context-type="${type}"`));
for(const phrase of["max-rounds:3,max-tool-calls:4,deadline-ms:100,max-same-signature:1","needs-input:needs_input,refused:refused,tool-error:tool_error","call-status:call_budget_exceeded,calls-executed:0","cycle=status:cycle_detected,calls-executed:1","application-owned-state,check-before-execute,monotonic-budgets"])assert.ok(lesson.includes(phrase));for(const phrase of["零基础兴趣","有基础求职","招聘信号","Python 3.11+","应用拥有循环"])assert.ok(lesson.includes(phrase));
const tests=spawnSync("python3",["-m","unittest","-v","test_bounded_tool_loop.py"],{cwd:resolve(root,"site-src/examples/agent-tool-calling/intelligent-learning-assistant-v17"),encoding:"utf8"});assert.equal(tests.status,0,tests.stdout+tests.stderr);assert.match(tests.stdout+tests.stderr,/Ran 8 tests/);console.log(JSON.stringify({valid:true,lesson_id:"agent-tool-calling-05",tests:8,terminal_states:9,bounded:true},null,2));
