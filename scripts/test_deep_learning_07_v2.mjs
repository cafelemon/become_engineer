import assert from"node:assert/strict";import{spawnSync}from"node:child_process";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lesson=readFileSync(resolve(root,"learning-paths/ai-foundation/deep-learning/07-checkpoint-rng-resume-exact-training.md"),"utf8");for(const type of["overview","concept","example","reproduce","modify","troubleshoot","project"])assert.match(lesson,new RegExp(`data-context-type="${type}"`));
for(const phrase of["checkpoint=model+optimizer+epoch+history+torch_rng_state","resume=epoch:8,rng_restored:true","equivalence=history:true,model:true,optimizer:true","verified-before-load","map_location:cpu,weights_only:true,schema:validated","atomic-write,trusted-local-artifact,exact-resume,no-test-data"])assert.ok(lesson.includes(phrase));for(const phrase of["随机种子","SHA-256","零基础兴趣","有基础求职","PyTorch 2.13.0"])assert.ok(lesson.includes(phrase));
const python=process.env.DL_PYTHON||resolve(root,".venv/bin/python"),tests=spawnSync(python,["-m","unittest","-v","test_checkpoint_lab.py"],{cwd:resolve(root,"site-src/examples/deep-learning/diagnosable-neural-network-v07"),encoding:"utf8",env:process.env});assert.equal(tests.status,0,tests.stdout+tests.stderr);assert.match(tests.stdout+tests.stderr,/Ran 8 tests/);console.log(JSON.stringify({valid:true,lesson_id:"deep-learning-07",tests:8,exact_resume:true,trust_boundary:true},null,2));
