#!/usr/bin/env node
import assert from "node:assert/strict";
import { mkdtempSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join, resolve } from "node:path";
import { spawnSync } from "node:child_process";
const root=resolve(import.meta.dirname,"..");const python=join(root,".venv/bin/python");const pyProject=join(root,"exercises/cs-core/traceable-graph-traversal-lab/python");const cppProject=join(root,"exercises/cs-core/traceable-graph-traversal-lab/cpp");const micro=join(root,"site-src/examples/algorithm-foundation/bfs_shortest_path_trace.py");const temp=mkdtempSync(join(tmpdir(),"be-cs-bfs-"));
function run(c,a,o={}){return spawnSync(c,a,{cwd:root,encoding:"utf8",...o});}function ok(r,l){assert.equal(r.status,0,`${l}\n${r.stdout}\n${r.stderr}`);}
try{const sample=run(python,[micro]);ok(sample,"BFS短轨迹失败");assert.match(sample.stdout,/discover=3 distance=2 parent=1/);assert.match(sample.stdout,/order=\[0, 1, 2, 3, 4\]/);assert.match(sample.stdout,/path_0_4=\[0, 1, 3, 4\]/);assert.match(sample.stdout,/path_0_6=\[\]/);const env={...process.env,PYTHONPATH:join(pyProject,"src")};ok(run(python,["-m","unittest","discover","-s","tests","-v"],{cwd:pyProject,env}),"Python测试失败");ok(run(python,["-m","mypy","--strict","src","tests"],{cwd:pyProject,env}),"mypy失败");const build=join(temp,"cpp-build");ok(run("cmake",["-S",cppProject,"-B",build,"-DCMAKE_BUILD_TYPE=Debug"]),"C++配置失败");ok(run("cmake",["--build",build,"--config","Debug","--parallel"]),"C++构建失败");ok(run("ctest",["--test-dir",build,"--build-config","Debug","--output-on-failure"]),"CTest失败");const cpp=join(build,"traceable_graph_traversal_lab");for(const mode of ["graph","bfs","dfs"]){const p=run(python,["-m","traceable_graph_traversal_lab",mode],{cwd:pyProject,env});const c=run(cpp,[mode]);ok(p,`Python ${mode}失败`);ok(c,`C++ ${mode}失败`);assert.equal(c.stdout.trim(),p.stdout.trim());}console.log("BFS、无权距离与最短路径验证通过");}finally{rmSync(temp,{recursive:true,force:true});}
