import{mkdirSync,writeFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lessonId="algorithm-deepening-10",title="Trie、KMP 与字符串匹配",path="learning-paths/algorithm-deepening/10-trie-kmp-string-matching/";
const contexts=[["overview-string-matching","overview","字符串匹配目标"],["concept-trie-prefix","concept","Trie 前缀"],["example-kmp-prefix","example","KMP 前缀函数"],["concept-kmp-search","concept","KMP 搜索"],["reproduce-string-v10","reproduce","运行字符串实验"],["modify-string-matching","modify","修改字符串契约"],["troubleshoot-string-matching","troubleshoot","字符串排错"],["project-pattern-lab-v10","project","约束模式实验 v1.0"],["career-string-matching","career","字符串追问"]].map(([id,type,title])=>({id,type,title,anchor:`#${id}`}));
const defs=[["trie-path","concept-trie-prefix","Trie 节点代表什么？","前缀树怎样共享路径","节点代表从根到此的字符前缀，多个词复用相同前缀边。","四词共 9 个节点。"],["terminal","concept-trie-prefix","路径存在为什么不等于完整词？","te 为什么不是词","只有 terminal 为真才是完整词；te 只是 tea 和 ten 的前缀。","contains_te=false。"],["autocomplete","concept-trie-prefix","怎样让 Trie 补全确定？","补全顺序如何固定","从前缀节点按字符排序 DFS，并在终止节点输出完整词。","te 得到 tea,ten。"],["duplicate","concept-trie-prefix","重复插入为何不增加节点？","Trie 插入幂等边界","已有字符边直接复用，只重复设置终止标记。","节点数保持 9。"],["prefix-meaning","example-kmp-prefix","KMP prefix 值代表什么？","最长真前后缀是什么","prefix[i] 是 pattern[0..i] 相等的最长真前缀和后缀长度。","ababd 得到 0,0,1,2,0。"],["fallback","example-kmp-prefix","KMP 失配时为什么沿 prefix 回退？","怎样避免文本回退","较短有效边界仍可能匹配当前字符，复用它而不重扫文本。","不变量 fallback-keeps-valid-border。"],["overlap","concept-kmp-search","KMP 怎样保留重叠匹配？","命中后 matched 怎么处理","记录下标后回退到 prefix[m-1]，不直接清零。","aaaaa/aaa 得到 0,1,2。"],["empty-pattern","reproduce-string-v10","空模式怎样处理？","KMP 空字符串契约","本课显式拒绝空模式，避免下标和无限匹配语义含糊。","测试期待 ValueError。"],["unicode-boundary","modify-string-matching","Python 和 C++ 字符下标一定相同吗？","Unicode 码点与 UTF-8 字节","Python 字符串按码点观察，当前 C++ string 按字节；非 ASCII 必须统一单位。","固定报告仅使用 ASCII。"],["pattern-v10","project-pattern-lab-v10","约束模式实验 v1.0 完成什么？","算法深化第十课项目是什么","完成 Trie 共享前缀、KMP 失败回退、重叠匹配和双语言固定证据。","十课连续项目闭环。"]];
const cards=defs.map(([id,c,q,a,answer,example],i)=>({id,lesson_id:lessonId,context_id:c,question:q,aliases:[a],keywords:[...new Set(`${q} ${a}`.replace(/[？?，、]/g," ").split(/\s+/).filter(Boolean))],diagnostic:`先判断“${a}”涉及 Trie 路径/终点还是 KMP 边界回退。`,hints:[`查看 #${c}。`,`运行 test_string_matching_trace.py。`],example,answer,source:{label:contexts.find(x=>x.id===c).title,href:`#${c}`},updated_at:"2026-07-23",recommended:i<6}));const cases=defs.flatMap(([id,,q,a])=>[{query:q.replace("？",""),expected_card:id},{query:a,expected_card:id}]);mkdirSync(resolve(root,"site-src/data/tutor"),{recursive:true});mkdirSync(resolve(root,"tests/tutor"),{recursive:true});writeFileSync(resolve(root,`site-src/data/tutor/${lessonId}.json`),`${JSON.stringify({version:2,lesson:{id:lessonId,title,path},contexts,cards},null,2)}\n`);writeFileSync(resolve(root,`tests/tutor/${lessonId}-search.json`),`${JSON.stringify({lesson_id:lessonId,cases,unknown:["陶瓷杯裂纹怎样修复","南极企鹅有哪些种类"]},null,2)}\n`);

