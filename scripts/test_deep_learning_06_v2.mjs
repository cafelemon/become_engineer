import assert from"node:assert/strict";import{spawnSync}from"node:child_process";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lesson=readFileSync(resolve(root,"learning-paths/ai-foundation/deep-learning/06-initialization-activation-gradient-diagnostics-clipping.md"),"utf8");for(const type of["overview","concept","example","reproduce","modify","troubleshoot","project"])assert.match(lesson,new RegExp(`data-context-type="${type}"`));
for(const phrase of["nonzero_rates:0.504|0.369,max:2.728024","global_gradient_norm=1.454179","before:1454.178728,after:1.000000","zero_initialization=hidden_gradients_stalled:true","activations-observed,layer-gradients-finite,clip-after-backward-before-step,root-cause-still-required"])assert.ok(lesson.includes(phrase));for(const phrase of["初始化","非有限","零基础兴趣","有基础求职","PyTorch 2.13.0"])assert.ok(lesson.includes(phrase));
const python=process.env.DL_PYTHON||resolve(root,".venv/bin/python"),tests=spawnSync(python,["-m","unittest","-v","test_diagnostics_lab.py"],{cwd:resolve(root,"site-src/examples/deep-learning/diagnosable-neural-network-v06"),encoding:"utf8",env:process.env});assert.equal(tests.status,0,tests.stdout+tests.stderr);assert.match(tests.stdout+tests.stderr,/Ran 8 tests/);console.log(JSON.stringify({valid:true,lesson_id:"deep-learning-06",tests:8,gradient_clipping:true,root_cause_boundary:true},null,2));
