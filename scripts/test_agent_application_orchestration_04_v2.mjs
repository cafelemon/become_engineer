import assert from"node:assert/strict";import{spawnSync}from"node:child_process";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,"..");const lesson=readFileSync(resolve(root,"learning-paths/llm-agent/agent-application-orchestration/04-human-approval-long-running-recovery.md"),"utf8");for(const type of["overview","concept","example","reproduce","modify","troubleshoot","project"])assert.match(lesson,new RegExp(`data-context-type="${type}"`));for(const phrase of["storage=postgresql:true","mock:false","approve、edit、reject","零基础兴趣","有基础求职"])assert.ok(lesson.includes(phrase),phrase);assert.ok(process.env.AGENT_POSTGRES_URL,"AGENT_POSTGRES_URL is required; real PostgreSQL cannot be replaced by a mock");const cwd=resolve(root,"site-src/examples/agent-application-orchestration/intelligent-learning-assistant-v34");const python=resolve(root,".venv/bin/python");const tests=spawnSync(python,["-m","unittest","-v","test_long_run_store.py"],{cwd,encoding:"utf8",env:{...process.env}});assert.equal(tests.status,0,tests.stdout+tests.stderr);assert.match(tests.stdout+tests.stderr,/Ran 8 tests/);const report=spawnSync(python,["long_run_store.py"],{cwd,encoding:"utf8",env:{...process.env}});assert.equal(report.status,0,report.stderr);assert.match(report.stdout,/idempotent:true/);console.log(JSON.stringify({valid:true,lesson_id:"agent-application-orchestration-04",tests:8,postgresql:true,mock:false},null,2));
