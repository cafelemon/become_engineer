import assert from"node:assert/strict";import{spawnSync}from"node:child_process";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lesson=readFileSync(resolve(root,"learning-paths/ai-foundation/machine-learning/05-cross-validation-hyperparameter-selection-final-test.md"),"utf8");for(const type of["overview","concept","example","reproduce","modify","troubleshoot","project","career"])assert.match(lesson,new RegExp(`data-context-type="${type}"`));for(const phrase of["candidate_count=6,fit_count=30","selected=decision-tree,max_depth=2","cv_balanced_accuracy=0.742,std=0.041","test_access=after-selection-once"])assert.ok(lesson.includes(phrase));for(const phrase of["GridSearchCV","StratifiedKFold","最终测试","零基础兴趣","有基础求职"])assert.ok(lesson.includes(phrase));const python=process.env.ML_PYTHON||resolve(root,".venv/bin/python");const tests=spawnSync(python,["-m","unittest","-v","test_model_selection_lab.py"],{cwd:resolve(root,"site-src/examples/machine-learning/structured-data-ml-system-v05"),encoding:"utf8",env:process.env});assert.equal(tests.status,0,tests.stdout+tests.stderr);assert.match(tests.stdout+tests.stderr,/Ran 8 tests/);console.log(JSON.stringify({valid:true,lesson_id:"machine-learning-05",tests:8,candidates:6,final_test:"after-selection-once"},null,2));
