import{mkdirSync,writeFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lessonId="ai-math-data-04",title="随机性、抽样、训练/验证划分与泄漏",path="learning-paths/ai-foundation/math-data/04-randomness-sampling-train-validation-leakage/";
const contexts=[["overview-reproducible-split","overview","可复现划分"],["concept-seed-local-rng","concept","种子与局部随机"],["concept-stratified-partitions","concept","分层分区"],["example-no-overlap-leakage","example","无交叉与泄漏"],["reproduce-split-v04","reproduce","运行划分实验"],["modify-split-policy","modify","修改划分策略"],["troubleshoot-split-leakage","troubleshoot","划分泄漏排错"],["project-reproducible-experiment-v04","project","实验系统 v0.4"],["career-split-leakage","career","实体泄漏追问"]].map(([id,type,title])=>({id,type,title,anchor:`#${id}`}));
const defs=[["seed","concept-seed-local-rng","随机种子解决什么问题？","固定 seed 有什么用","相同算法、稳定输入顺序和相同种子可复现同一伪随机序列。","样例 seed=20260723。"],["seed-limit","concept-seed-local-rng","固定种子为什么不等于结果稳健？","可复现等于可靠吗","种子只复现一次抽样，不能消除样本偏差或证明换划分后结果稳定。","还需重复实验或时间外验证。"],["local-rng","concept-seed-local-rng","为什么使用局部 Random？","不要修改全局随机状态","独立 Random 实例隔离本实验的随机状态，不影响应用其他模块。","测试验证全局状态不变。"],["stable-input","concept-seed-local-rng","同种子为何仍可能得到不同划分？","输入顺序影响 shuffle 吗","伪随机操作还依赖初始顺序，所以先按稳定 sample_id 排序。","反转输入仍得到相同 Split。"],["stratified","concept-stratified-partitions","分层划分怎样保留类别？","每个标签内部抽样","先按标签分组并分别抽取验证样本，再组合训练和验证分区。","验证标签计数为 0:1,1:1。"],["small-class","concept-stratified-partitions","少数类只有一个样本怎么办？","一个样本能同时训练验证吗","同一数据不能同时充当训练与独立验证，本课拒绝无法覆盖两边的类别。","不能复制样本伪造覆盖。"],["no-overlap","example-no-overlap-leakage","怎样证明训练验证无交叉无丢失？","split 集合不变量","交集必须为空，并集必须等于全部稳定样本 ID。","固定输出 overlap=0。"],["entity-leakage","example-no-overlap-leakage","ID 不重叠为何仍可能泄漏？","同一用户多记录跨分区","不同记录可能属于同一实体或采集事件，需要按实体组划分而不只是行 ID。","同用户图片跨分区会虚高指标。"],["time-split","troubleshoot-split-leakage","什么时候不应随机划分？","时间序列怎样验证","若生产面对未来数据，应使用过去训练、未来验证来暴露时间漂移。","随机划分可能混入未来模式。"],["experiment-v04","project-reproducible-experiment-v04","实验系统 v0.4 新增什么？","AI 数学数据第四课项目","新增局部种子、分层训练验证划分、类别计数及无交叉无丢失证据。","重复 ID 和不可分类别会拒绝。"]];
const cards=defs.map(([id,c,q,a,answer,example],i)=>({id,lesson_id:lessonId,context_id:c,question:q,aliases:[a],keywords:[...new Set(`${q} ${a}`.replace(/[？?，、/]/g," ").split(/\s+/).filter(Boolean))],diagnostic:`先判断“${a}”涉及随机复现、类别覆盖还是泄漏边界。`,hints:[`查看 #${c}。`,`运行 test_split_lab.py。`],example,answer,source:{label:contexts.find(x=>x.id===c).title,href:`#${c}`},updated_at:"2026-07-23",recommended:i<7}));
const cases=defs.flatMap(([id,,q,a])=>[{query:q.replace("？",""),expected_card:id},{query:a,expected_card:id}]);mkdirSync(resolve(root,"site-src/data/tutor"),{recursive:true});mkdirSync(resolve(root,"tests/tutor"),{recursive:true});writeFileSync(resolve(root,`site-src/data/tutor/${lessonId}.json`),`${JSON.stringify({version:2,lesson:{id:lessonId,title,path},contexts,cards},null,2)}\n`);writeFileSync(resolve(root,`tests/tutor/${lessonId}-search.json`),`${JSON.stringify({lesson_id:lessonId,cases,unknown:["怎样冲泡手冲咖啡","为什么猫会打呼噜"]},null,2)}\n`);
