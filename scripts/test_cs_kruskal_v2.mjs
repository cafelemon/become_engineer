#!/usr/bin/env node
import assert from "node:assert/strict";import{mkdtempSync,rmSync}from"node:fs";import{tmpdir}from"node:os";import{join,resolve}from"node:path";import{spawnSync}from"node:child_process";
const root=resolve(import.meta.dirname,"..");const python=join(root,".venv/bin/python");const pyProject=join(root,"exercises/cs-core/traceable-spanning-forest-lab/python");const cppProject=join(root,"exercises/cs-core/traceable-spanning-forest-lab/cpp");const micro=join(root,"site-src/examples/algorithm-core/kruskal_cycle_trace.py");const temp=mkdtempSync(join(tmpdir(),"be-cs-kruskal-"));
function run(c,a,o={}){return spawnSync(c,a,{cwd:root,encoding:"utf8",...o});}function ok(r,l){assert.equal(r.status,0,`${l}\n${r.stdout}\n${r.stderr}`);}
try{const s=run(python,[micro]);ok(s,"Kruskal短轨迹失败");assert.match(s.stdout,/accept 5-6@-1 components=6/);assert.match(s.stdout,/reject 0-1@4 same_root=0/);assert.match(s.stdout,/reject 1-3@5 same_root=0/);assert.match(s.stdout,/reject 2-4@6 same_root=0/);assert.match(s.stdout,/total_weight=7 components=2 edge_count=5/);const env={...process.env,PYTHONPATH:join(pyProject,"src")};ok(run(python,["-m","unittest","discover","-s","tests","-v"],{cwd:pyProject,env}),"Python测试失败");ok(run(python,["-m","mypy","--strict","src","tests"],{cwd:pyProject,env}),"mypy失败");const build=join(temp,"cpp-build");ok(run("cmake",["-S",cppProject,"-B",build,"-DCMAKE_BUILD_TYPE=Debug"]),"C++配置失败");ok(run("cmake",["--build",build,"--config","Debug","--parallel"]),"C++构建失败");ok(run("ctest",["--test-dir",build,"--build-config","Debug","--output-on-failure"]),"CTest失败");const cpp=join(build,"traceable_spanning_forest_lab");for(const mode of["dsu","kruskal","prim"]){const p=run(python,["-m","traceable_spanning_forest_lab",mode],{cwd:pyProject,env});const c=run(cpp,[mode]);ok(p,`Python ${mode}失败`);ok(c,`C++ ${mode}失败`);assert.equal(c.stdout.trim(),p.stdout.trim());}console.log("Kruskal、环检测与最小生成森林验证通过");}finally{rmSync(temp,{recursive:true,force:true});}

