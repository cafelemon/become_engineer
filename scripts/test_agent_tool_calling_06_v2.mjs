import assert from"node:assert/strict";import{spawnSync}from"node:child_process";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lesson=readFileSync(resolve(root,"learning-paths/llm-agent/tool-calling-workflow/06-fixed-adversarial-evaluation-regression-delivery.md"),"utf8");for(const type of["overview","concept","example","reproduce","modify","troubleshoot","project"])assert.match(lesson,new RegExp(`data-context-type="${type}"`));
for(const phrase of["protocol=version:1,cases:8","case-mix=benign:2,safety:3,budget:3","outcome-accuracy:1.00,benign-outcome:1.00,call-accounting:1.00","rejection-accuracy:1.00,unsafe-executions:0","compliance:1.00,zero-extra-execution:true","same-protocol-required:true,no-regression:true","eight-cases-not-production-quality"])assert.ok(lesson.includes(phrase));for(const phrase of["零基础兴趣","有基础求职","招聘信号","Python 3.11+","8 例满分不是生产质量"])assert.ok(lesson.includes(phrase));
const tests=spawnSync("python3",["-m","unittest","-v","test_tool_workflow_eval.py"],{cwd:resolve(root,"site-src/examples/agent-tool-calling/intelligent-learning-assistant-v18"),encoding:"utf8"});assert.equal(tests.status,0,tests.stdout+tests.stderr);assert.match(tests.stdout+tests.stderr,/Ran 8 tests/);const report=spawnSync("python3",["tool_workflow_eval.py"],{cwd:resolve(root,"site-src/examples/agent-tool-calling/intelligent-learning-assistant-v18"),encoding:"utf8"});assert.equal(report.status,0,report.stderr);const fingerprint=report.stdout.match(/fingerprint:([a-f0-9]{12})/);assert.ok(fingerprint);assert.ok(lesson.includes(`fingerprint:${fingerprint[1]}`),`lesson fingerprint must be ${fingerprint[1]}`);console.log(JSON.stringify({valid:true,lesson_id:"agent-tool-calling-06",tests:8,cases:8,gate:"passed",fingerprint:fingerprint[1]},null,2));
