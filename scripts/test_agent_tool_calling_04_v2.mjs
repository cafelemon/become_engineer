import assert from"node:assert/strict";import{spawnSync}from"node:child_process";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lesson=readFileSync(resolve(root,"learning-paths/llm-agent/tool-calling-workflow/04-controlled-side-effects-confirmation-idempotency.md"),"utf8");for(const type of["overview","concept","example","reproduce","modify","troubleshoot","project"])assert.match(lesson,new RegExp(`data-context-type="${type}"`));
for(const phrase of["risk:write,permission:learning_plan:write,default:deny","error:confirmation_required,writes:0","retry=status:ok,replayed:true,writes:1","bound-arguments:true,expires:true,single-use:true","same-payload:replay,different-payload:conflict","default-deny,write-once,replay-safe"])assert.ok(lesson.includes(phrase));for(const phrase of["零基础兴趣","有基础求职","招聘信号","Python 3.11+","分布式 exactly-once"])assert.ok(lesson.includes(phrase));
const tests=spawnSync("python3",["-m","unittest","-v","test_controlled_write.py"],{cwd:resolve(root,"site-src/examples/agent-tool-calling/intelligent-learning-assistant-v16"),encoding:"utf8"});assert.equal(tests.status,0,tests.stdout+tests.stderr);assert.match(tests.stdout+tests.stderr,/Ran 8 tests/);console.log(JSON.stringify({valid:true,lesson_id:"agent-tool-calling-04",tests:8,writes:1,replay_safe:true},null,2));
