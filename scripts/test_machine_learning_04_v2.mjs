import assert from"node:assert/strict";import{spawnSync}from"node:child_process";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lesson=readFileSync(resolve(root,"learning-paths/ai-foundation/machine-learning/04-decision-tree-splits-overfitting-complexity.md"),"utf8");for(const type of["overview","concept","example","reproduce","modify","troubleshoot","project","career"])assert.match(lesson,new RegExp(`data-context-type="${type}"`));for(const phrase of["unconstrained=depth:7,nodes:29,leaves:15","constrained=max_depth:3,min_samples_leaf:5","capacity_reduced=true","training_gap_unconstrained=0.433"])assert.ok(lesson.includes(phrase));for(const phrase of["Gini","过拟合","min_samples_leaf","零基础兴趣","有基础求职"])assert.ok(lesson.includes(phrase));const python=process.env.ML_PYTHON||resolve(root,".venv/bin/python");const tests=spawnSync(python,["-m","unittest","-v","test_decision_tree_lab.py"],{cwd:resolve(root,"site-src/examples/machine-learning/structured-data-ml-system-v04"),encoding:"utf8",env:process.env});assert.equal(tests.status,0,tests.stdout+tests.stderr);assert.match(tests.stdout+tests.stderr,/Ran 8 tests/);console.log(JSON.stringify({valid:true,lesson_id:"machine-learning-04",tests:8,capacity_reduced:true,tree_rules:true},null,2));
