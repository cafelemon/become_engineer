#!/usr/bin/env node
import assert from "node:assert/strict";import{mkdtempSync,rmSync}from"node:fs";import{tmpdir}from"node:os";import{join,resolve}from"node:path";import{spawnSync}from"node:child_process";
const root=resolve(import.meta.dirname,"..");const python=join(root,".venv/bin/python");const pyProject=join(root,"exercises/cs-core/traceable-priority-shortest-path-lab/python");const cppProject=join(root,"exercises/cs-core/traceable-priority-shortest-path-lab/cpp");const micro=join(root,"site-src/examples/algorithm-core/min_heap_trace.py");const temp=mkdtempSync(join(tmpdir(),"be-cs-heap-"));function run(c,a,o={}){return spawnSync(c,a,{cwd:root,encoding:"utf8",...o});}function ok(r,l){assert.equal(r.status,0,`${l}\n${r.stdout}\n${r.stderr}`);}
try{const s=run(python,[micro]);ok(s,"最小堆短轨迹失败");assert.match(s.stdout,/built=\[1, 3, 9, 7, 5\] comparisons=5 swaps=3/);assert.match(s.stdout,/removed=1 remaining=\[3, 5, 9, 7\] comparisons=3 swaps=1/);const env={...process.env,PYTHONPATH:join(pyProject,"src")};ok(run(python,["-m","unittest","discover","-s","tests","-v"],{cwd:pyProject,env}),"Python测试失败");ok(run(python,["-m","mypy","--strict","src","tests"],{cwd:pyProject,env}),"mypy失败");const build=join(temp,"cpp-build");ok(run("cmake",["-S",cppProject,"-B",build,"-DCMAKE_BUILD_TYPE=Debug"]),"C++配置失败");ok(run("cmake",["--build",build,"--config","Debug","--parallel"]),"C++构建失败");ok(run("ctest",["--test-dir",build,"--build-config","Debug","--output-on-failure"]),"CTest失败");const cpp=join(build,"traceable_priority_shortest_path_lab");for(const mode of["heap","queue","dijkstra"]){const p=run(python,["-m","traceable_priority_shortest_path_lab",mode],{cwd:pyProject,env});const c=run(cpp,[mode]);ok(p,`Python ${mode}失败`);ok(c,`C++ ${mode}失败`);assert.equal(c.stdout.trim(),p.stdout.trim());}console.log("二叉最小堆、隐式树与堆不变量验证通过");}finally{rmSync(temp,{recursive:true,force:true});}
