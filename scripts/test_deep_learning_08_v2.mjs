import assert from"node:assert/strict";import{spawnSync}from"node:child_process";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lesson=readFileSync(resolve(root,"learning-paths/ai-foundation/deep-learning/08-eval-inference-schema-manifest-delivery.md"),"utf8");for(const type of["overview","concept","example","reproduce","modify","troubleshoot","project"])assert.match(lesson,new RegExp(`data-context-type="${type}"`));
for(const phrase of["artifact=diagnosable-neural-network-v08,version:1","input_schema=signal_a:float32,signal_b:float32,extra:false,finite:true","negative_case=label:negative,probabilities:0.750260|0.249740","inference=eval:true,inference_mode:true,requires_grad:false,deterministic:true","load=map_location:cpu,weights_only:true,strict:true","state-dict-only,manifest-validated,trusted-local-artifact,no-network,no-personal-data"])assert.ok(lesson.includes(phrase));for(const phrase of["类别顺序","SHA-256","零基础兴趣","有基础求职","65 项真实 PyTorch 测试"])assert.ok(lesson.includes(phrase));
const python=process.env.DL_PYTHON||resolve(root,".venv/bin/python"),tests=spawnSync(python,["-m","unittest","-v","test_delivery_lab.py"],{cwd:resolve(root,"site-src/examples/deep-learning/diagnosable-neural-network-v08"),encoding:"utf8",env:process.env});assert.equal(tests.status,0,tests.stdout+tests.stderr);assert.match(tests.stdout+tests.stderr,/Ran 9 tests/);console.log(JSON.stringify({valid:true,lesson_id:"deep-learning-08",tests:9,inference_contract:true,tamper_rejection:true},null,2));
