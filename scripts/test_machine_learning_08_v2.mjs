import assert from"node:assert/strict";import{spawnSync}from"node:child_process";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lesson=readFileSync(resolve(root,"learning-paths/ai-foundation/machine-learning/08-model-persistence-inference-contract-delivery-acceptance.md"),"utf8");for(const type of["overview","concept","example","reproduce","modify","troubleshoot","project","career"])assert.match(lesson,new RegExp(`data-context-type="${type}"`));for(const phrase of["sha256_length=64","roundtrip_probability_match=true","invalid_schema=rejected:missing-or-additional-feature","tampered_artifact=rejected:checksum-mismatch"])assert.ok(lesson.includes(phrase));for(const phrase of["pickle","SHA-256","推理 Schema","零基础兴趣","有基础求职"])assert.ok(lesson.includes(phrase));const python=process.env.ML_PYTHON||resolve(root,".venv/bin/python");const tests=spawnSync(python,["-m","unittest","-v","test_delivery_lab.py"],{cwd:resolve(root,"site-src/examples/machine-learning/structured-data-ml-system-v08"),encoding:"utf8",env:process.env});assert.equal(tests.status,0,tests.stdout+tests.stderr);assert.match(tests.stdout+tests.stderr,/Ran 9 tests/);console.log(JSON.stringify({valid:true,lesson_id:"machine-learning-08",tests:9,checksum:"before-unpickle",schema:"exact"},null,2));
