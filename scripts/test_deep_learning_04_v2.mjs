import assert from"node:assert/strict";import{spawnSync}from"node:child_process";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lesson=readFileSync(resolve(root,"learning-paths/ai-foundation/deep-learning/04-mini-batch-sgd-learning-rate-training-loop.md"),"utf8");
for(const type of["overview","concept","example","reproduce","modify","troubleshoot","project"])assert.match(lesson,new RegExp(`data-context-type="${type}"`));
for(const phrase of["initial_train=loss:0.841436,accuracy:0.069","final_train=loss:0.016031,accuracy:1.000","history=epochs:30,steps:180,rows_per_epoch:72","learning_rate_zero=no_parameter_change:true","zero-backward-step,all-train-batches-once,history-recorded,validation-untouched"])assert.ok(lesson.includes(phrase));
for(const phrase of["mini-batch","SGD","零基础兴趣","有基础求职","PyTorch 2.13.0"])assert.ok(lesson.includes(phrase));
const python=process.env.DL_PYTHON||resolve(root,".venv/bin/python"),tests=spawnSync(python,["-m","unittest","-v","test_training_lab.py"],{cwd:resolve(root,"site-src/examples/deep-learning/diagnosable-neural-network-v04"),encoding:"utf8",env:process.env});assert.equal(tests.status,0,tests.stdout+tests.stderr);assert.match(tests.stdout+tests.stderr,/Ran 8 tests/);console.log(JSON.stringify({valid:true,lesson_id:"deep-learning-04",tests:8,steps:180,validation_untouched:true},null,2));
