import assert from"node:assert/strict";import{spawnSync}from"node:child_process";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lesson=readFileSync(resolve(root,"learning-paths/llm-agent/tool-calling-workflow/03-multiple-calls-call-id-correlation-partial-failure.md"),"utf8");for(const type of["overview","concept","example","reproduce","modify","troubleshoot","project"])assert.match(lesson,new RegExp(`data-context-type="${type}"`));
for(const phrase of["batch=calls:3,max:4,unique-call-ids:true,execution:sequential","call_unknown:error:unknown_tool","out-of-order-restored:true","successes:2,errors:1,batch-aborted:false","one-output-per-call,call-id-not-index,stable-order"])assert.ok(lesson.includes(phrase));for(const phrase of["零基础兴趣","有基础求职","招聘信号","Python 3.11+","多调用、乱序和并发"])assert.ok(lesson.includes(phrase));
const tests=spawnSync("python3",["-m","unittest","-v","test_multi_call_batch.py"],{cwd:resolve(root,"site-src/examples/agent-tool-calling/intelligent-learning-assistant-v15"),encoding:"utf8"});assert.equal(tests.status,0,tests.stdout+tests.stderr);assert.match(tests.stdout+tests.stderr,/Ran 8 tests/);console.log(JSON.stringify({valid:true,lesson_id:"agent-tool-calling-03",tests:8,max_calls:4,partial_failure:"isolated"},null,2));
