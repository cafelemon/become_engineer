import assert from"node:assert/strict";import{spawnSync}from"node:child_process";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lesson=readFileSync(resolve(root,"learning-paths/ai-foundation/deep-learning/05-validation-curves-overfitting-weight-decay-dropout.md"),"utf8");for(const type of["overview","concept","example","reproduce","modify","troubleshoot","project"])assert.match(lesson,new RegExp(`data-context-type="${type}"`));
for(const phrase of["best_epoch:50,train_accuracy:0.828,final_validation_loss:0.571875","best_epoch:204,train_accuracy:0.750,final_validation_loss:0.488543","selected=regularized,rule=lowest-final-validation-loss","dropout=train_stochastic:true,eval_stable:true","same-data-initialization-budget,validation-only-selection,best-state-retained,test-untouched"])assert.ok(lesson.includes(phrase));
for(const phrase of["过拟合","weight decay","零基础兴趣","有基础求职","PyTorch 2.13.0"])assert.ok(lesson.includes(phrase));const python=process.env.DL_PYTHON||resolve(root,".venv/bin/python"),tests=spawnSync(python,["-m","unittest","-v","test_validation_lab.py"],{cwd:resolve(root,"site-src/examples/deep-learning/diagnosable-neural-network-v05"),encoding:"utf8",env:process.env});assert.equal(tests.status,0,tests.stdout+tests.stderr);assert.match(tests.stdout+tests.stderr,/Ran 8 tests/);console.log(JSON.stringify({valid:true,lesson_id:"deep-learning-05",tests:8,selected:"regularized",test_untouched:true},null,2));
