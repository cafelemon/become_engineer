import assert from"node:assert/strict";import{spawnSync}from"node:child_process";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=resolve(import.meta.dirname,".."),lesson=readFileSync(resolve(root,"learning-paths/ai-foundation/machine-learning/01-problem-features-target-majority-baseline.md"),"utf8");for(const type of["overview","concept","example","reproduce","modify","troubleshoot","project","career"])assert.match(lesson,new RegExp(`data-context-type="${type}"`));for(const phrase of["rows=120","train_rows=90,class_counts=0:60,1:30","validation_rows=30,class_counts=0:20,1:10","baseline_accuracy=0.667","target-excluded,stratified-split,validation-untouched"])assert.ok(lesson.includes(phrase));for(const phrase of["DummyClassifier","合成数据","目标列","零基础兴趣","有基础求职"])assert.ok(lesson.includes(phrase));const python=process.env.ML_PYTHON||resolve(root,".venv/bin/python");const tests=spawnSync(python,["-m","unittest","-v","test_baseline_lab.py"],{cwd:resolve(root,"site-src/examples/machine-learning/structured-data-ml-system-v01"),encoding:"utf8",env:process.env});assert.equal(tests.status,0,tests.stdout+tests.stderr);assert.match(tests.stdout+tests.stderr,/Ran 7 tests/);console.log(JSON.stringify({valid:true,lesson_id:"machine-learning-01",tests:7,synthetic_data:true,stratified_baseline:true},null,2));
