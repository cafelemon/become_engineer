import{existsSync}from"node:fs";import{chromium}from"../site-src/examples/web-engineering/learning-dashboard-v12/node_modules/playwright/index.mjs";const executablePath=process.env.PLAYWRIGHT_CHROMIUM_EXECUTABLE||"/Users/jiafei/Library/Caches/ms-playwright/chromium_headless_shell-1223/chrome-headless-shell-mac-arm64/chrome-headless-shell";if(!existsSync(executablePath))throw new Error("Chromium missing");const browser=await chromium.launch({headless:true,executablePath});const base=process.env.SITE_BASE_URL||"http://127.0.0.1:8765";const root=`${base}/learning-paths/llm-agent/agent-application-orchestration`;const slugs=["01-workflow-router-agent-boundaries","02-session-context-summary-memory-retrieval","03-task-decomposition-budget-replanning","04-human-approval-long-running-recovery","05-end-to-end-evaluation-trace-fault-localization","06-security-deployment-rollback-delivery-evidence"];const modes=[{name:"desktop-light",viewport:{width:1440,height:900},colorScheme:"light",reducedMotion:"no-preference",javaScriptEnabled:true},{name:"mobile-dark-reduced",viewport:{width:390,height:844},colorScheme:"dark",reducedMotion:"reduce",javaScriptEnabled:true},{name:"mobile-no-js",viewport:{width:390,height:844},colorScheme:"light",reducedMotion:"reduce",javaScriptEnabled:false}];let checks=0;for(const mode of modes){const context=await browser.newContext(mode);for(const slug of slugs){const page=await context.newPage();await page.goto(`${root}/${slug}/`,{waitUntil:"domcontentloaded"});const audit=await page.evaluate(()=>({title:document.querySelector("h1")?.textContent?.trim(),overflow:document.documentElement.scrollWidth>document.documentElement.clientWidth,profiles:["零基础兴趣","有基础兴趣","零基础求职","有基础求职"].every(text=>document.body.innerText.includes(text)),project:document.body.innerText.includes("智能学习助手"),source:document.body.innerText.includes("来源与版本")}));if(!audit.title||audit.overflow||!audit.profiles||!audit.project||!audit.source)throw new Error(`${mode.name} ${slug}:${JSON.stringify(audit)}`);checks++;await page.close();}await context.close();}const context=await browser.newContext();const page=await context.newPage();await page.goto(`${root}/${slugs.at(-1)}/`,{waitUntil:"networkidle"});const launcher=page.locator('button[aria-label="打开学习助教"]');await launcher.focus();await page.keyboard.press("Enter");await page.locator('.be-tutor-panel[role="dialog"]').waitFor({state:"visible"});await page.keyboard.press("Escape");const focused=await page.evaluate(()=>document.activeElement?.getAttribute("aria-label")||"");if(focused!=="打开学习助教")throw new Error(`focus:${focused}`);await context.close();await browser.close();console.log(JSON.stringify({valid:true,page_checks:checks,modes:modes.map(m=>m.name),keyboard_focus:focused},null,2));
