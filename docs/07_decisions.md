# 决策记录

## 2026-07-15: 四类画像共用一张总课程表与四类训练资产

### 决策

完整课程地图作为唯一总课程表。四类画像不复制课程正文，而是从基础课程、新手操作包、深化挑战包和求职训练包中进行规则组合；岗位专项继续复用共同主干和项目证据。

### 原因

四套独立课程会让相同知识、示例、错误修订和 URL 长期漂移。按资产组合既能让从零学习者获得足够细的操作指引，也能让有基础者直接进入原理和工程权衡，并为求职路线增加机考与证据表达而不污染兴趣路线。

### 影响

- 总课程建设顺序固定为共同基础、四路线纵向样板、CS 主干、Web 应用、AI/LLM、岗位专项。
- 规划中内容必须标注未建设；课程结构、来源登记或题型规划不能冒充已开放课程。
- 求职训练必须依附代码、测试、图、日志或故障复盘；不维护孤立八股手册。

## 2026-07-15: 个性化采用四类入口与四维能力的可解释静态编排

### 决策

首页由小码提供一次可跳过的入学测评。用户看到“从零／已有基础 × 兴趣／求职”四类画像，路线引擎同时使用工程工具、编程、算法 CS 和项目工程四维能力补修。首次确认后才精简导航，完整路线和全部内容始终可访问。

### 原因

只维护四套课程会造成正文重复和长期漂移；只给一个能力总分又会误判“会编程但不会 Git”等混合状态。统一课程加静态编排既能复用内容，也能让学习者理解为什么保留、跳过或暂未显示某个模块。

### 影响

- 首页测评与单课知识库助教逻辑隔离，只复用宠物视觉和静态部署边界。
- 浏览器仅保存画像摘要与导航状态；原始答案、账号和云端同步继续不进入 V1。
- 未显示原因成为稳定产品状态：已掌握、目标不匹配、前置未满足、内容未建设。
- 首批求职层面向通用软件工程，岗位专项在共同能力样板通过验证后再建设。

## 2026-07-15: Python 装饰器课采用显式组合与分阶段文件提交

### 决策

Python 核心第 5 课使用 `ParamSpec`、`TypeVar` 和 `wraps` 实现类型安全调用追踪器，并用 `@contextmanager` 将审计写入升级为“临时文件写入成功后替换正式文件”。核心报告函数不永久挂载追踪器，调用者按需显式组合。

### 原因

显式组合可以展示装饰器价值，又避免模块导入时创建全局日志状态或改变标准输出。分阶段写入为自定义上下文管理器提供真实、可测试的资源边界，并能在失败时保护已有审计内容。

### 影响

- `build_report()` 与 `write_audit_snapshot()` 的签名和主报告文本保持不变。
- 正式课程数增加到 26，知识库增至 216 张卡、430 条固定问法。
- `ContextDecorator`、`ExitStack`、异步装饰器和异步上下文管理不进入本课；下一步转向包结构、CLI、配置与日志。

## 2026-07-15: Python 对象与资源配对课采用数据类和原生 with

### 决策

Python 核心第 4 课将双语言报告器从 `TypedDict` 迁移为 `@dataclass` 对象，并使用原生文件上下文管理器完成审计导出。课程不同时展开自定义 `__enter__`、`__exit__`、`ExitStack`、异步上下文管理或通用装饰器设计。

### 原因

本课需要与 C++ RAII 建立可观察的对象与资源对照。数据类、对象方法和文件 `with` 已能形成完整任务闭环；继续加入函数装饰器和自定义协议会超过单课颗粒度，削弱迁移与排错练习。

### 影响

- Python 与 C++ 审计格式一致，主报告字符串继续作为跨语言回归契约。
- 正式课程数增加到 25，知识库增至 207 张卡、412 条固定问法。
- 下一节明确为装饰器与自定义上下文管理器；产品仍不引入模型 API、账号或后端。

## 2026-07-15: 课程生产 Skill 与 C++ 对象资源样板

### 决策

将已验收的任务驱动课程、小码知识库、检索测试和 MkDocs 接入流程固化为仓库内 `$become-engineer-course-authoring` Skill，并在个人 Codex Skills 安装经校验副本。以 C++“对象、引用、指针、生命周期与 RAII”作为正式第五课进行端到端验证。

### 原因

课程生产重复涉及任务结构、稳定锚点、知识卡、检索问法、阶段作品和导航接入。把它们保留在维护者记忆中容易漏项；把流程收敛为 Skill 可以在后续写课时保持可验证的一致结构。

### 影响

- 仓库 Skill 是唯一维护源；个人副本仅在校验后同步，更新必须从仓库复制。
- 第五课以安全失败分析替代执行悬空引用等未定义行为，并以 `ofstream` 的成功/失败测试解释 RAII。
- 正式课程数从 23 节增加到 24 节；不改变静态检索、无账号、无模型 API 的产品边界。

## 2026-07-15: 已验收任务驱动与知识库助教推广到全部正式课程

### 决策

Python 第一课及小码同学的交互与内容验收后，将该模式一次推广到工程基础、Python 起步、C++ 核心和 Python 核心共 23 节正式课程。每节课采用 5–7 步任务结构，并配置独立的公开知识库与固定检索测试。

### 原因

学习者需要在整条已开放路线中获得一致的“先完成、再理解、再修改、再排错”节奏。已有课程已包含可复现代码、练习、错误排查和阶段作品，将其重组为连续任务并延续既有成果线，比新增统一大项目更贴近真实学习过程。

### 影响

- 助教覆盖全部 23 节正式课程；每课至少 8 张审核卡、16 条固定问法，Top 3 命中率至少 80%。
- 保持确定性检索、分层提示、未知问题降级和仅本机保存提示进度的边界；不引入模型 API、账号、后端或跨设备同步。
- 课程 URL、MkDocs Material、导航、阶段作品目录和公开内容边界保持不变。

## 2026-07-15: 任务驱动课程与知识库像素助教先做单课样板

### 决策

编程课程采用“任务先行、即时反馈、按需讲解、主动修改、排错、迁移验收”作为新基线。首个样板重构 Python 第一课，并在该页接入由公开结构化知识卡驱动的原创像素宠物助教。

### 原因

现有课程已经具备练习和验收，但知识讲解仍主要位于实践之前。先让零基础学习者完成可观察任务，再根据操作和错误提供最少必要知识，可以更快形成正反馈；确定性知识库比直接接入大模型更容易控制答案、来源、成本和教学节奏。

### 影响

- V1 允许自由输入，但只返回已审核知识卡和明确来源，未命中时诚实降级。
- 回答默认逐层展开，不直接泄露完整答案。
- `localStorage` 只保存提示层级和面板状态，不保存原始提问；V1 不登录、不跨设备同步。
- 像素宠物是交互入口和状态反馈，不替代课程内容，无 JavaScript 时正文仍完整。
- 样板效果通过首次成功耗时、完成率、排错和迁移任务判断，不以聊天次数代替学习证据。

## 2026-07-14: 素材收集与加工目录改为完全本地

### 决策

`content-inbox/` 和 `source-materials/` 整体保留在本地工作区，不再被 Git 跟踪，也不进入 MkDocs 页面和搜索。公开仓库只保留独立整理后的课程、笔记、练习、项目、必要引用和治理文档。

### 原因

候选来源、清洗记录和原始文件服务于内容生产，不是学习者需要阅读的正式内容。只忽略部分子目录仍可能让后续新素材或加工文件被误提交，因此改为目录整体隔离，并增加常见二进制原件和大型数据/模型格式的全局保护。

### 影响

- 本地素材实体保留，只从当前 Git 索引撤下；不重写历史、不强制推送。
- 早期“content-inbox 说明入库”和“source-materials README 入库”的决定被本决策取代。
- 正式课程图片、小型 CSV/JSON/TXT 和 Markdown 继续允许入库。
- 学习地图不再链接本地来源目录，公开内容只保留实际使用的必要引用。

## 2026-07-14: 学习路线样板成为视觉基线并推广到三类页面

### 决策

已通过人工评审的学习路线阶段 Stepper 作为 Become Engineer 的正式视觉基线。本轮只推广到 README 首页、完整课程地图和《学习方法》第一课，形成入口页、地图页和课程页样板。

### 原因

当前风格已经验证了方向感、阅读节奏和可交互感，同时保持 Markdown 内容完整。先覆盖三类代表页面可以验证复用边界，避免一次性改写全部课程造成大范围返工。

### 影响

- 共享组件继续使用语义化 HTML 与 CSS，不引入前端框架或学习状态。
- 三层课程结构同时使用文字、颜色和布局表达，不能只依赖颜色。
- 其他页面仅在后续扩写或明确评审后迁移。
- 项目设计基线记录在 `.ai/design/system.md`。

## 2026-07-14: 项目治理文档仅在 GitHub 仓库查看

### 决策

公开学习站点只保留首页、开始学习和项目三个顶部入口。`docs/` 继续入库维护，但从 MkDocs 页面构建和搜索索引中完全排除。

### 原因

PRD、架构、进度和决策面向项目维护，不属于学习者的主任务。隐藏导航但继续构建仍会让这些页面通过搜索和直接 URL 暴露，因此采用完整构建排除。

### 影响

- 公开页面引用治理文档时改用 GitHub `main` 分支链接。
- 原站内 `docs/` URL 停止提供，课程和项目 URL 不变。
- README 首页转为项目简介、作者、GitHub 仓库与学习入口，不再展示内部治理入口。

## 2026-07-14: 学习体验采用渐进增强并先做单页样板

### 决策

学习体验前端增强继续使用 MkDocs Material、Markdown、语义化 HTML 和 CSS。首个样板只改造学习路线顶部的默认路线总览；样板验收后再依次推进首页、完整课程地图和课程阅读页。只有确有筛选或本地状态需求时才使用少量原生 JavaScript。

### 原因

当前主要问题是学习方向感和阅读节奏不足，而不是缺少完整前端应用。先验证一个真实样板，可以在保持内容源、URL 和发布方式稳定的前提下确定视觉语言，避免把未确认的设计批量复制到全部课程。

### 影响

- V1 不引入 React、Vue、Docusaurus、账号、后端或云端学习状态。
- 页面增强必须支持键盘、移动端、深浅色和无 JavaScript 阅读。
- 《学习方法》将作为后续课程阅读体验样板；现有课程不进行一次性视觉重写。
- 详细范围、版本与验收以[学习体验前端增强计划](09_learning_experience_frontend_plan.md)为准。

## 2026-07-14: 首页承担路线导航，开始学习直接进入课程

> 后续修订：本决策中的四入口口径已被同日“项目治理文档仅在 GitHub 仓库查看”取代；首页分支、直接开课和左右目录分工继续有效。

### 决策

公开站点继续保留首页、开始学习、项目和项目文档四个顶部入口。学习路线与完整课程地图归入首页分支；点击“开始学习”直接进入第一节“学习方法”。桌面端左侧只显示可折叠课程树，右侧独立显示当前页面标题目录。

### 原因

路线说明和课程地图用于帮助学习者建立全局认知，不应阻挡已经决定开始学习的人。把长页面标题目录与课程树混在同一侧栏，会造成层级混淆和过长滚动，也让课程分组缺少明确的收起体验。

### 影响

- 工程基础课程说明不进入课程翻页顺序，第一节课程提供阶段说明与验收入口。
- MkDocs Material 继续作为站点外壳，课程 URL 和 Markdown 内容源保持不变。
- 本轮只修复信息架构与目录行为；路线 Stepper、课程筛选和学习进度状态作为后续渐进增强单独设计。

## 2026-07-13: AI协作优先但必须保留人工验证

### 决策

后续课程默认允许并鼓励使用AI生成脚手架、样板代码、测试、文档、图表和排错建议。AI输出统一视为待验证输入，学习者必须完成审阅、运行、解释、主动修改和验证；核心算法、内存机制、模型步骤及权限安全逻辑设置手动检查点。

### 原因

AI时代的工程能力不等于机械手敲全部代码，而是能够清楚定义问题、有效使用工具、判断生成结果并对最终行为负责。只禁止AI会脱离真实工程工作，只接受AI又会失去理解与安全边界。

### 影响

- 课程不要求公开完整私人对话，只记录任务、约束、AI产出摘要、人工修改和验证证据。
- AI生成代码只有在实际运行并通过课程验收后才能进入公开示例。
- 官方资料、实验和测试分别承担事实、行为和回归验证，不能以AI回答替代。
- 新规范从后续新增或扩写内容开始执行，已有课程本轮不批量返工。

## 2026-07-13: 内容采用适配式呈现与三级实践

### 决策

课程根据概念、编程、算法、Web、AI/LLM和项目类型选择Mermaid、表格、代码、输出与实验，不设置机械图表数量。实践统一分为正文内微练习、连续3-6节形成的阶段作品和跨课程长期项目。

### 原因

纯文本难以表达流程、状态和交互，但无意义图表同样增加理解负担。项目若与每节课一一对应会迅速碎片化，无法体现知识在完整系统中的持续加码。

### 影响

- 每张图必须回答一个具体问题，并由正文解释；可复制代码必须具备完整复现信息。
- 单模块阶段作品进入 `exercises/`；推进既有长期项目时只增加项目里程碑，不重复建目录。
- 纯理论课程组可以跳过阶段作品，但必须说明原因并提供替代验收。
- 算法阶段作品优先采用题集、可视化或分析工具，Web、AI和LLM优先形成可运行、可观察和可评估功能。

## 2026-07-13: 语言之后采用三层课程架构

### 决策

CS、Web、AI和LLM/Agent统一使用必修主干、推荐深化和专业选修三层结构。小白统一路线只定义必修顺序，完整课程地图负责展示全部选修和跨主线依赖。

### 原因

扁平知识清单无法同时满足零基础学习和长期深入。把所有内容设为必修会拖慢默认路线，把全部深度都写成普通选修又会失去前置和验收约束。

### 影响

- 必修主干验收继续控制下一主线解锁。
- 推荐深化不阻塞默认路线，但可以成为专业选修的前置。
- 专业选修必须说明用途、前置、时机、实践、验收和跳过影响。
- 工程基础和语言路线本轮保持现状，不按三层结构重新改写。
- 本轮只建立架构，不编写正文、不下载素材、不创建练习或项目目录。

## 2026-07-13: 算法机考属于CS推荐深化验收

### 决策

CS必修主干覆盖数据结构、基础算法和复杂度；双指针、滑动窗口、贪心、动态规划、图算法等题型深化及限时机考放入推荐深化，不单独建设求职课程或项目。

### 原因

机考题适合验证算法能力，但只按题目名称组织会把课程变成刷题目录。按能力族和递进验收组织，可以同时服务工程理解和求职验证。

### 影响

- 题型按字符串、栈队列、树、贪心区间、动态规划、回溯和综合输入输出等能力族组织。
- 机考验收顺序为基础实现、单一题型、混合题、限时模拟和错题复盘。
- 限时模拟可使用主力语言，代表性题目使用Python/C++对照实现。
- C++内存指针与双指针算法技巧在课程中明确区分。

## 2026-07-13: 语言主线采用长期能力阶梯

### 决策

Python起步以独立完成可维护的小型程序为验收终点；Python/C++双主修采用核心语言、工程化、并发网络、运行原理与性能、领域应用五层能力阶梯，并贯穿后续项目持续深化。

### 原因

只列变量、控制流、STL或CMake等知识点，容易让学习者把“看懂基础语法”误认为掌握语言。成为工程师需要持续经历设计、实现、测试、调试、测量和复盘。

### 影响

- 当前两篇Python起步正文保持不变，后续课程按更高完成标准继续建设。
- CS最小核心只要求核心语言和基础工程化验收，不要求先学完全部高级主题。
- 并发、网络、性能和领域能力在Web、AI、工业控制及系统项目中持续回链语言课程。

## 2026-07-13: 工业控制与实时通信作为CS可选专业分支

### 决策

在CS深化中新增完整但非必修的工业控制与实时通信分支，顺序为PLC与控制基础、Beckhoff/TwinCAT、ADS、DDS、仿真系统集成和真实设备联调。

### 原因

PLC运行模型、工业通信和实时数据分发依赖C++、操作系统、并发、网络和序列化等CS能力，但不是所有Web、AI或LLM学习者的共同前置。将其设为可选专业分支，可以保留完整深度而不拖慢默认路线。

### 影响

- 有相关需求的学习者在完成C++基础工程化和CS前置后进入；其他学习者可以跳过。
- 实践先在软件仿真中验证PLC、ADS和DDS数据链路，再扩展到Beckhoff PLC/IPC及真实I/O。
- DDS按OMG Data Distribution Service理解；具体实现将在课程建设时根据平台、许可和维护状态选择。
- 安全PLC、现场总线和生产控制属于后续专业深化，仿真结果不能作为生产部署证明。
- 当前只登记项目候选，不创建空项目目录或代码。

## 2026-06-25: 仓库采用公开学习平台定位

### 决策

本仓库定位为公开学习平台，覆盖工程开发、数据库、AI、LLM 和 Agent 等方向。

### 原因

项目既服务个人长期学习，也希望形成面向公众可阅读、可复用、可纠错的知识沉淀。

### 影响

- 文档需要面向外部读者，而不是只写给自己。
- 资源收集需要保留来源和质量判断。
- 笔记需要从个人草稿整理为公开表达。

## 2026-06-25: 个人学习资料不进入公开仓库

### 决策

本地 `个人学习/` 目录作为私有资料区，加入 `.gitignore`，不提交到 GitHub。

### 原因

个人学习资料可能包含未整理草稿、下载资料、隐私信息和版权风险内容，不适合公开。

### 影响

- 公开内容需要从私有目录中筛选、整理后再进入仓库。
- 提交前必须检查 `git status`，确认私有目录未被跟踪。

## 2026-06-25: 初期以 Markdown 仓库为主

### 决策

项目初期不急于搭建复杂站点，先使用 Markdown 和目录结构沉淀内容。

### 原因

当前最重要的是建立内容体系、筛选标准和长期维护节奏。

### 影响

- 后续可以平滑迁移到 VitePress、Docusaurus 或 MkDocs。
- 现阶段优先完善内容结构和质量标准。

## 2026-06-25: 采用四层六线结构

### 决策

项目采用四层六线结构组织公开内容。

四层为学习地图、学习路线、项目实践、资源评测。六线为工程基础、编程语言、Web 全栈、CS 核心、AI 基础、LLM/Agent。

### 原因

单纯资源列表容易堆链接，单纯课程路线又难以承载个人学习沉淀。四层六线可以同时保留全局导航、阶段路线、实践验证和资源判断。

### 影响

- 新增内容必须先映射到主线和内容层级。
- `resources/` 记录资源事实，`reviews/` 记录资源判断。
- 从 `个人学习/` 提炼的内容优先进入 `publications/`、`notes/`、`projects/` 或 `reviews/`。

### 后续修订

2026-06-25 的“面向小白采用唯一顺序化路线”决策补充了本决策：六条主线继续用于分类，但不再作为并列可选的学习顺序。

## 2026-06-25: 新增 content-inbox 作为素材收纳层

> 后续修订：该目录仍承担本地素材收纳职责，但“说明与清单入库”的口径已于 2026-07-14 取消，目录整体由 Git 忽略。

### 决策

新增 `content-inbox/`，用于登记未整理素材、所属主线、处理状态和下一步动作。

### 原因

当前素材数量多且来源混杂，直接进入正式目录会导致公开内容质量不稳定。Inbox 可以先承接混乱资料，再经过去重、去广告、纠错和项目筛选后进入正式目录。

### 影响

- Inbox 不保存访问密码、token、下载资料原件或受版权保护内容。
- 素材必须先标注状态，再决定进入 `resources/`、`reviews/`、`notes/`、`projects/` 或 `publications/`。
- 项目推动是优先原则，但工程基础和纯理论内容不强制独立项目化。

## 2026-06-25: 原始素材放入 source-materials 而非个人学习区

> 后续修订：该目录继续作为本地素材加工区，但 `README.md` 不再入库，目录整体由 Git 忽略。

### 决策

新增 `source-materials/` 作为项目级素材加工区。`README.md` 入库，`raw/`、`working/`、`exports/` 作为本地 ignored 子目录。

### 原因

外部资料是项目内容生产来源，不属于个人学习区；但原文、广告、旧内容和中间产物也不适合直接公开。

### 影响

- 原始抓取内容放入 `source-materials/raw/`。
- 清洗、去重、纠错过程文件放入 `source-materials/working/`。
- 只有二次加工后的公开内容进入正式目录。

## 2026-06-25: 内容加工以知识单元而非原文章为单位

### 决策

首个样品不逐篇清洗发布，而是将 4 篇 Function Calling 素材合并为一个新的 Tool Calling 知识单元。

### 原因

原素材存在大量主题重复、旧接口、广告和不安全示例。逐篇修改会保留原有结构缺陷，也难以形成清晰学习路径。

### 影响

- 加工区记录来源、重复关系、问题和取舍。
- 公开内容采用独立结构和表达。
- 一个知识单元可以产出笔记与聚焦练习，或服务于某个项目里程碑。

## 2026-06-25: 首个 Tool Calling 练习采用离线优先

### 决策

安全销售查询练习默认使用离线模拟 Tool Call，OpenAI Responses API 作为可选适配器。

### 原因

学习者不应因为缺少 API Key、调用额度或特定模型权限而无法验证核心流程。离线模式也更适合自动测试参数和权限边界。

### 影响

- 核心项目只使用 Python 标准库。
- 真实模型 SDK 和环境变量限制在项目目录。
- 不把付费 API 调用作为验收条件。

## 2026-06-25: 数据工具暴露业务能力而非任意 SQL

### 决策

样品只提供 `get_monthly_sales`，不提供 `execute_sql`。

### 原因

模型生成任意 SQL 后直接执行会扩大读取、写入、资源消耗和提示注入风险。固定业务工具更容易做权限、参数和结果控制。

### 影响

- SQL 结构由代码维护，模型参数只作为绑定值。
- SQLite 使用只读连接。
- 写操作和高风险工具未来默认需要更严格权限及人工确认。

## 2026-06-25: 历史决策：单一大模型素材的适用边界

### 决策

《大模型全部学习笔记》只用于精做 Python、AI 基础和 LLM/Agent。工程基础、Web 全栈、CS 核心和 C++ 只登记缺口，不调整六条主线。

### 原因

素材在机器学习、深度学习和 Agent 上体量很大，但 Git、工程方法、Web、CS 和 C++ 内容零散，不能把标题命中当成完整课程。

### 影响

- 新素材先映射到六条主线和能力依赖图。
- `llama.cpp` 不计入 C++ 基础。
- FastAPI、Flask 和 Codex 案例不计入完整 Web 或工程课程。

### 后续修订

CS DIY和FastAPI等来源已完成目录登记，因此公开口径改为“来源已登记、尚未加工到该阶段”。单一素材的覆盖不足仍然成立，但不再表述为整条主线没有来源。

## 2026-06-25: 项目跨笔记演进

### 决策

项目不与单篇笔记一一对应。一个项目由多个里程碑组成，每个里程碑关联前置课程、笔记、代码产出和验收结果。

### 原因

真实能力需要多个知识点共同完成。为每篇笔记创建项目会产生大量碎片，框架教程也会被误当成独立成果。

### 影响

- 建立五个项目主干，不为每个框架和章节建立项目。
- 项目型笔记必须说明推进了哪个里程碑。
- 纯理论笔记说明能力去向即可。
- 聚焦实验进入 `exercises/`。

## 2026-06-25: 基础课完整前置并在项目中持续回链

### 决策

工程、语言、数学和模型原理中必要的基础内容先形成完整课程；项目实施时继续链接回这些基础课，并按具体问题深化。

### 原因

完全项目驱动容易使知识碎片化，学完基础后完全脱离项目又会失去关联。两者需要同时存在。

### 影响

- 项目入口必须列出前置课程。
- 项目笔记必须列出实际使用的基础知识。
- 尚未加工完成的基础课程必须标明来源状态、加工进度和所在阶段，不得伪装为已完成课程。

### 后续修订

本决策中的“完整前置”已被“面向小白采用唯一顺序化路线”细化：语言前先完成必要工程基础，测试、CI、部署等能力在有代码和项目后持续深化。

## 2026-06-25: 大型外部资料先建目录再按需取用

### 决策

CS 自学指南和 FastAPI 官方文档当前只做目录级梳理，不下载完整站点或仓库。具体路线章节或项目里程碑需要时，再拉取最小必要页面和示例。

### 原因

现有问题不是资源不足，而是资料分散且缺少统一路线。提前下载大量内容会增加重复、更新和版权管理成本。

### 影响

- `content-inbox/external-source-catalog.md` 记录来源结构、用途、许可和触发条件。
- CS 自学指南主要用于课程筛选，不复制其完整路线。
- FastAPI 主要作为官方事实来源和项目实现参考。
- 每次取用记录 URL、日期、版本、许可和选取原因。
- 外部原文如需本地处理，仍进入 ignored 的 `source-materials/raw/`。

## 2026-06-25: 面向小白采用唯一顺序化路线

### 决策

全仓库采用唯一默认学习顺序：

```text
工程基础前置
  -> Python 起步
    -> Python/C++ 双主修
      -> CS 最小核心
        -> Web 与 CS 深化并行
          -> AI 基础
            -> LLM/Agent
```

Java作为完整补充路线，JavaScript/TypeScript主要在Web阶段进入。

### 原因

六条并列主线适合维护者组织内容，却要求小白自己判断顺序。项目希望读者从第一步开始照着操作，也能逐阶段完成。

### 影响

- `learning-paths/beginner-roadmap.md` 成为唯一顺序基准。
- 六条主线负责分类和阶段内容，不独立定义全局顺序。
- 工程基础分为语言前必修和项目中持续深化。
- Python先建立共同编程概念，再与C++并行主修。
- CS最小核心完成后才启动Web，并在Web期间深化CS。
- 项目按阶段解锁，专项地图和素材目录不能提前分配后期任务。

## 2026-06-25: Docker、算法和数据库采用分层学习口径

### 决策

- Docker进入工程基础，但语言前只要求最小认知：镜像、容器、端口、挂载、环境变量和常用查看命令。
- Dockerfile、docker compose、数据库容器、服务部署、监控和回滚在后续项目中逐步深化。
- 算法拆成两层：CS核心学习通用算法与复杂度；AI基础学习机器学习、深度学习、NLP/Transformer和强化学习等模型算法。
- 数据库实践优先使用 PostgreSQL 和 `psql`；MySQL作为常见生产数据库对照；SQLite只用于轻量本地练习或特定样例。

### 原因

Docker过早深入会拖慢小白进入编程，但后续部署和复现离不开容器认知。算法如果只写一个词，容易混淆刷题、数据结构和模型训练。数据库如果只写SQL，也会忽略真实工程中连接、迁移、事务、索引和部署工具链的差异。

### 影响

- 工程基础课程需要加入Docker最小认知章节，但不把容器部署设为语言前门槛。
- CS核心课程必须覆盖通用算法，不把机器学习内容提前塞进CS阶段。
- AI基础课程必须明确模型算法的前置和边界，NLP/Transformer作为LLM/Agent关键桥梁。
- Web与CS数据库内容默认围绕PostgreSQL/psql组织，再补MySQL差异。

## 2026-06-25: 小白课程单元采用可执行文档契约

### 决策

每个正式课程单元必须包含前置知识、明确顺序、命令或示例、练习或检查、常见错误与排查、客观验收和下一步。

### 原因

只有概念总结和资源链接仍然要求学习者自行组织学习，不能满足“挨着做也能行”的目标。

### 影响

- 课程模板增加解锁条件、错误排查和阶段导航。
- 项目章节持续回链工程、语言和CS基础。
- 后续课程正文需要实际运行验证，不能只做目录规划。

## 2026-06-25: 公开阅读体验采用 MkDocs Material

### 决策

本仓库采用 MkDocs Material 生成公开网站。Markdown 继续作为唯一内容源，`mkdocs.yml` 直接以仓库根目录作为文档源，并通过排除规则保护私有目录和原素材加工区。

### 原因

内容会持续增长，单靠 GitHub 文件列表不适合新学习者按阶段阅读。MkDocs Material 可以提供导航、目录、搜索、深浅色主题和 GitHub Pages 发布能力，同时不改变现有 Markdown 写作方式。

### 影响

- 新增 `mkdocs.yml`、`requirements.txt` 和 GitHub Actions 发布 workflow。
- 网站发布目标为 `https://cafelemon.github.io/become_engineer/`。
- `个人学习/`、`source-materials/raw/`、`source-materials/working/`、`source-materials/exports/` 不进入站点构建。
- 后续新增正式课程时需要同步更新 `mkdocs.yml` 导航。
