# 进度记录

## 2026-07-17：Python 起步 7 节完成 V2 迁移

### 已完成

- 将正式第七课《异常、基本调试和最小自动化测试》迁移为 V2 项目整合课：从“程序正常结束但进度显示 150%”进入，区分语法错误、运行时异常和错误结果，再沿 traceback、输入边界、调试工具和单元测试完成修复闭环。
- 删除旧五步任务开场，保留并重组明确异常捕获、`raise`、`else/finally`、`repr()`、`type()`、`breakpoint()`、断言边界、`unittest`、临时目录、标准错误和退出码等必要知识。
- 新增三份短例子和课程验证脚本，真实复现错误结果与 traceback，运行 2 项微型测试和阶段作品 14 项测试，并验证导入无副作用、输入只读、成功退出码 0 与非法输入退出码 1。
- 学习进度报告器正式收口到 v1.0；七节课程形成从单条变量档案到多模块、可排错、可回归程序的连续版本线。
- 小码迁移为 23 张 V2 语义知识卡和 46 条自然问法，覆盖问题分类、traceback、输入校验、窄范围捕获、调试、测试隔离、退出码和项目表达。

### 验收

- 本课 46 条问法 Top 1 / Top 3 均为 100%，2 条未知问题正确降级；38 节 V1 正式课程与 13 个冻结样板保持兼容。
- 正式迁移统计更新为：17 节已迁移、34 节待迁移、4 节样板已通过待迁移；工程基础 10/10、Python 起步 7/7。
- 下一批进入真正的 CS 起步，先将已验收的《数据如何在程序中表示》样板迁移到正式第一课，再连续处理操作成本、字符串编码和二维网格。

## 2026-07-17：Python 起步推进到模块、导入与虚拟环境

### 已完成

- 将正式第六课《模块、导入和虚拟环境》迁移为 V2 编程起步课：先展示报告输出不变、内部完成分工，再解释模块职责、两种导入方式、导入顶层执行、入口保护和单向依赖。
- 学习进度报告器推进到 v0.6：读取写入、业务计算、文本表现和启动编排分别进入 `data_io.py`、`analysis.py`、`reporting.py` 与 `main.py`；从任意当前目录运行仍能找到项目数据。
- 新增四模块标准库示例与离线验证，真实检查导入无副作用、重构前后输出相同、项目根目录稳定、`venv` 解释器归属和本地 `json.py` 遮蔽标准库的失败路径。
- 小码迁移为 23 张 V2 语义知识卡和 46 条自然问法，覆盖职责划分、导入来源、入口保护、虚拟环境、模块遮蔽、循环导入和项目表达。

### 验收

- 本课代码测试全部通过，不联网、不安装第三方包；真实虚拟环境在临时目录创建并验证后清理。
- 46 条问法 Top 1 / Top 3 均为 100%，2 条未知问题正确降级；其余 39 节 V1 正式课程与 13 个冻结样板保持兼容。
- 正式迁移统计更新为：16 节已迁移、35 节待迁移、4 节样板已通过待迁移；Python 起步批次 6/7。
- 下一步迁移《异常、基本调试和最小自动化测试》，把报告器推进到 v1.0，并完成 Python 起步连续课程组。

## 2026-07-17：Python 起步推进到文件、路径与 JSON

### 已完成

- 将正式第五课《文件、路径、JSON 和简单目录操作》迁移为 V2 编程起步课：从 JSON 输入生成文本报告的实际结果进入，依次解释当前工作目录、Path、UTF-8、JSON 解析、受限扫描和分层失败。
- 学习进度报告器推进到 v0.5：输入固定在 `data/study_records.json`，结果固定写到 `output/study_report.txt`；修改完成小时不再需要改 Python 代码。
- 新增 JSON 文本微型例子和完整文件报告例子；离线测试在临时目录验证 UTF-8 往返、输入只读、受限 glob、稳定输出、缺失文件和坏 JSON，不污染仓库示例目录。
- 正文把 `FileNotFoundError`、`UnicodeDecodeError`、`JSONDecodeError`、`KeyError` 与输出失败分别放回路径、读取、解析、字段和写入层，不用宽泛异常捕获隐藏 traceback。
- 小码迁移为 22 张 V2 语义知识卡和 44 条自然问法，覆盖 cwd、路径组合、编码、JSON 语法、loads/dumps、输入输出边界、扫描范围、失败定位和项目表达。

### 验收

- 两类示例通过正常、缺失输入与无效 JSON 测试；输入运行前后字节一致，扫描只覆盖受控 data 目录。
- 本课 44 条问法 Top 1 / Top 3 均为 100%，2 条未知问题正确降级；其余 40 节 V1 正式课程与 13 个冻结样板保持兼容。
- 正式迁移统计更新为：15 节已迁移、36 节待迁移、4 节样板已通过待迁移；Python 起步批次 5/7。
- 下一步迁移《模块、导入和虚拟环境》，把已能工作的报告器按读取、分析、报告和入口拆分为 v0.6。

## 2026-07-17：Python 起步推进到字符串与常用容器

### 已完成

- 将正式第四课《字符串、列表、字典、集合和元组》迁移为 V2 编程起步课：从三条学习记录的真实汇总进入，用同一份数据解释字符串、列表、字典、集合和元组各自解决的问题。
- 保留索引、切片、字符串清理、列表增删与排序、元组解包、字典必填/可选字段、集合运算和嵌套容器等必要深度；删除旧五步任务开场与孤立方法表堆叠。
- 新增标签文本清理、多记录报告和列表共享三份可运行示例；离线测试覆盖固定汇总、字符串不可变、唯一标签、直接赋值、外层复制与内层共享。
- 学习进度报告器推进到 v0.4：外层列表保存多条记录，字典表达具名字段，集合生成唯一标签，元组返回固定汇总；学习者还需验证空列表、重复标签、超额完成和缺失可选字段。
- 小码迁移为 22 张 V2 语义知识卡和 44 条自然问法，覆盖容器选择、文本处理、列表方法、元组、字典键、集合、复制边界和项目表达。

### 验收

- 三份示例全部通过固定输出与共享边界测试；自动测试不联网、不安装第三方包。
- 本课 44 条问法 Top 1 / Top 3 均为 100%，2 条未知问题正确降级；其余 41 节 V1 正式课程与 13 个冻结样板保持兼容。
- 正式迁移统计更新为：14 节已迁移、37 节待迁移、4 节样板已通过待迁移；Python 起步批次 4/7。
- 下一步迁移《文件、路径、JSON 和简单目录操作》，把内存记录保存到文件并形成报告器 v0.5。

## 2026-07-17：Python 起步推进到函数与作用域

### 已完成

- 将正式第三课《函数、参数、返回值和作用域》迁移为 V2 编程起步课：从报告器 v0.3 的两条实际输出进入，再依次解释定义与调用、形参与实参、位置/关键字/默认参数、`return`、局部作用域和函数职责。
- 保持正式 URL 与 `python-basics-03` 助教标识；删除旧五步任务开场，用十二个语义学习区域组织理解、例子、复现、主动修改、排错、项目、深化与求职表达。
- 新增三份标准库示例和离线测试，固定验证报告器输出、默认与关键字参数、`return`/`print()` 差异，以及局部变量越界后的 `NameError`；浏览器运行器继续只承载固定数据微型代码。
- 学习进度报告器推进到 v0.3：`calculate_progress()` 计算比例，`build_status()` 生成状态，`build_report_line()` 组合显示；学习者还需自己增加 `can_review()` 并覆盖三组布尔输入。
- 小码迁移为 20 张 V2 语义知识卡和 40 条自然问法，覆盖函数调用、参数绑定、默认值、返回值、作用域、常见报错、项目约定与重构表达。

### 验收

- 三份示例全部通过固定输出与失败路径测试；测试明确不联网、不安装第三方包。
- 本课 40 条问法 Top 1 / Top 3 均为 100%，2 条未知问题正确降级；其余 42 节 V1 正式课程与 13 个冻结样板保持兼容。
- 正式迁移统计更新为：13 节已迁移、38 节待迁移、4 节样板已通过待迁移；Python 起步批次 3/7。
- 下一步迁移《字符串、列表、字典、集合和元组》，把单条报告扩展为多条记录，形成报告器 v0.4。

## 2026-07-17：Python 起步推进到条件与循环

### 已完成

- 将《条件、循环、布尔逻辑》从五步任务和分散语法练习，重写为“报告器状态结果 → 比较与布尔值 → `if` 分支 → v0.2 复现 → 三档状态 → 布尔组合 → 行动清单 → 有上限重试 → 错误恢复 → 项目与求职表达”。
- 报告器 v0.2 延续第一课文件，增加未开始／进行中／已完成状态、剩余小时、行动清单和复盘条件；状态顺序用 0、3、5 三组数据覆盖。
- 新增三个离线示例，分别验证固定报告、刚好达标边界、提前 `break` 和三次耗尽；故意死循环只作为本地临时练习，不进入正式示例与提交。
- 小码迁移为 21 张 V2 语义知识卡和 42 条自然问法，覆盖比较、分支、缩进、`elif` 顺序、布尔组合、`range()` 边界、循环退出和项目说明。

### 验收

- 三个示例的固定报告、三类分支、提前退出和次数耗尽全部通过；测试明确报告不联网、不安装第三方包。
- 本课 42 条问法 Top 1 / Top 3 均为 100%，2 条未知问题正确降级；其余 43 节 V1 正式课程与 13 个冻结样板保持兼容。
- 桌面与 390px 浏览器回归无横向溢出；分支图在手机端改为单列，浏览器示例实际运行成功，小码面板为全屏并能随语义区域更新推荐问题。
- 正式迁移统计更新为：12 节已迁移、39 节待迁移、4 节样板已通过待迁移；Python 起步批次 2/7。
- 下一步迁移《函数、参数、返回值和作用域》，把状态判断与报告输出整理成可复用函数，形成报告器 v0.3。

## 2026-07-17：Python 起步第一课完成 V2 迁移

### 已完成

- 将《变量、基本类型、输入输出》从固定七步任务改为“学习档案结果 → 名称与值 → 短例子 → 本机复现 → 换成自己的数据 → 输入与转换 → 错误恢复 → 项目 v0.1”的连续讲解。
- 保留正式 URL 和 `python-basics-01` 助教标识，吸收已验收样板的图解与浏览器微型运行器；正式项目仍要求在本地终端运行，页面运行器不支持 `input()`、文件或第三方包，也不保存代码。
- 新增三份标准库示例，覆盖固定输出、四种基础类型、交互输入、整数转换和 `ValueError`；离线测试同时验证成功与失败路径。
- 学习进度报告器从第一课开始建立版本线：v0.1 保存一条学习档案，后续六课继续加入判断、函数、容器、文件、模块和测试。
- 小码迁移为 18 张 V2 语义知识卡和 36 条自然问法，覆盖变量绑定、重新赋值、基础类型、浏览器与本机差异、输入、转换、常见错误和项目表达。

### 验收

- 三份 Python 示例固定输出、类型检查、交互成功和 `ValueError` 失败路径均通过；测试明确报告不联网、不安装第三方包。
- 本课 36 条问法 Top 1 / Top 3 均为 100%，2 条未知问题正确降级；其余 44 节 V1 正式课程与 13 个冻结样板保持兼容。
- 桌面与 390px 浏览器回归无横向溢出；变量图、输入流程和运行器在手机端改为单列，浏览器示例实际运行成功，小码面板保持全屏与焦点返回。
- 正式迁移统计更新为：11 节已迁移、40 节待迁移、4 节样板已通过待迁移；Python 起步批次 1/7。
- 下一步迁移《条件、循环、布尔逻辑》，把学习档案升级为能够根据完成情况给出状态的 v0.2。

## 2026-07-17：工程基础 10 节正式完成 V2 迁移

### 已完成

- 将工程基础最后一课《验证习惯》重写为“阶段结果 → 结论、检查与判断 → 通过／失败／未验证 → 跨平台阶段检查 → 最小失败复现 → 学习者自己写验收 → 手工检查升级为自动测试 → 工程学习工作台 v1.0”。
- 新手不需要一开始就理解测试框架：课程先让学习者写清楚“我想证明什么、运行什么、实际看到什么、下一步怎么办”，再把稳定检查逐步交给脚本。
- 阶段检查覆盖项目根目录、关键文件、Git 历史、忽略规则、远程地址、虚拟环境和 Docker 状态；Docker Engine 是可选项，未安装时记录“未验证”，不阻塞进入 Python。
- 新增离线阶段验收脚本，在临时目录建立真实 Git 仓库和 3 次提交，确认 `.venv` 被忽略、工作区干净，并安全复现缺失文件错误后恢复；脚本不联网、不调用 Docker。
- 小码切换为 21 张 V2 语义知识卡和 42 条自然问法，覆盖验收结论、跨平台检查、Git、虚拟环境、Docker、失败复现、自动化与项目表达。
- 工程基础十课现在由同一条工程学习工作台项目线串联，从 v0.1 的学习记录逐节演进到 v1.0 的阶段验收。

### 验收

- 离线脚本确认项目根目录、3 次提交、忽略环境、干净工作区、安全失败复现和恢复全部通过；输出明确记录 `network_used: false`、`docker_invoked: false`。
- 正式迁移统计更新为：10 节已迁移、40 节待迁移、5 节样板已通过待迁移；工程基础批次 10/10 完成。
- 下一步进入 Python 起步 7 节，第一课吸收已验收的变量、类型与输入输出样板，同时保持正式 URL、课程 ID 和学习进度报告器项目线。

## 2026-07-17：正式 V2 迁移推进到《Docker最小认知》

### 已完成

- 将工程基础第九课从“先列五步任务，再解释概念”重写为“第一次容器结果 → CLI／Engine／镜像／容器关系 → 安装与状态检查 → 可选真实运行 → 命令拆解 → 端口／挂载／配置 → 主动修改 → 分层排错 → 容器边界 → 工程学习工作台 v0.9”。
- 纠正 `docker --version` 的判断范围：它只能证明 CLI 可用；课程使用 `docker version` 的 Client／Server 结果继续判断 Engine，避免新手把后台未启动误判为安装完全失败。
- 建立两条完整学习路径：默认路径不安装 Docker、不下载镜像，只完成概念与命令阅读；可选路径在用户确认联网和本机存储影响后运行官方 `hello-world`，并用 `--rm` 观察容器与镜像生命周期差异。
- 端口示例默认绑定 `127.0.0.1`，bind mount 只开放项目内最小目录并使用 `readonly`，环境变量只使用非敏感 `APP_MODE`；真实令牌、密码、私人路径与内部镜像地址不进入命令、截图或 Git。
- 新增离线命令解析测试，检查回环端口、宿主机／容器端口、只读挂载、普通配置和镜像字段；自动测试不调用 Docker、不联网、不读取密钥。
- 小码切换为 21 张 V2 语义知识卡和 42 条自然问法，覆盖安装、Client／Server、镜像拉取、`--rm`、端口、挂载、环境变量、常见错误和项目提交。

### 验收

- 固定 Docker 命令与主动修改版本均通过解析，确认只绑定回环地址、只读挂载、无秘密字段；测试明确报告 `docker_invoked: false` 和 `network_used: false`。
- 本课 42 条问法进入全量检索回归，2 条无关问题继续走不猜测降级；正式课程的 V1/V2 自动发现边界保持不变。
- 正式迁移统计更新为：9 节已迁移、41 节待迁移、5 节样板已通过待迁移。
- 下一步迁移《验证习惯》，把工程学习工作台 v0.1–v0.9 中分散的检查动作整理为第一个阶段收口。

## 2026-07-17：正式 V2 迁移推进到《开发环境》

### 已完成

- 将工程基础第八课从“版本、位置、PATH、依赖五步任务”重写为“环境指纹 → 代码／解释器／依赖关系 → PATH 查找 → 平台安装与定位 → 项目虚拟环境 → 激活与直接调用 → 环境记录 → 现象排错 → 工程学习工作台 v0.8”。
- Windows 按 Python 官方 Install Manager 与 `python`／`py list` 路径说明，macOS 使用 `python3` 和官方安装包，Linux 保留发行版官方包管理边界；三类平台都要求核对版本和实际可执行文件位置。
- 在 `learning-workspace` 根目录真实创建 `.venv`，不依赖激活脚本也能调用项目 Python；用 `sys.executable`、`sys.prefix != sys.base_prefix` 和 `python -m pip --version` 共同证明解释器与包管理器属于当前项目。
- `notes/environment-check.md` 只保存经过脱敏的环境摘要，`.venv/`、完整 PATH、私人绝对路径与凭据继续留在本机并由 Git 忽略。
- 新增离线环境测试，在临时目录创建真实虚拟环境并验证解释器、prefix 与 pip 归属，不联网、不安装第三方包；小码切换为 20 张 V2 语义知识卡和 40 条自然问法。

### 验收

- Python 3.11 基础解释器成功创建项目虚拟环境，项目 Python 报告隔离状态，pip 来自 `.venv`；临时环境测试完成后自动清理。
- 本课 40 条问法 Top 1 / Top 3 均为 100%，2 条未知问题正确降级；其余正式课程和 13 个冻结样板保持兼容。
- 正式迁移统计更新为：8 节已迁移、42 节待迁移、5 节样板已通过待迁移。
- 下一步迁移《Docker 最小认知》，把本地解释器和依赖隔离继续扩展到镜像、容器、端口与挂载的最小系统关系。

## 2026-07-17：正式 V2 迁移推进到《GitHub 远程协作》

### 已完成

- 将工程基础第七课从“创建仓库后连续执行五个任务”重写为“原仓库与 clone 的同一提交 → 本地／远程关系 → 读懂 URL → 推送前隐私检查 → 创建空仓库 → 配置 origin → 首次 push → 主动修改 → 独立 clone → 错误定位 → 上游关系 → 工程学习工作台 v0.7”。
- 延续上一课的 `learning-workspace` 和两次提交，默认建议私人仓库；公开前要求同时检查当前文件和提交历史，明确 `.gitignore` 无法清除已经提交的密钥或隐私。
- 分开解释 `origin`、远程 URL、`origin/main`、push 和 clone；认证路径只采用 GitHub 官方 HTTPS／SSH 边界，不要求把账号密码或 token 写入命令、文件、截图或聊天。
- 新增离线远程验证脚本，使用本机裸仓库模拟 origin，完成首次 push、第三次提交、普通 push 和独立 clone；自动测试不访问 GitHub，也不读取账号与凭据。
- 小码切换为 V2 语义知识库，共 17 张卡、34 条自然问法，覆盖空远程、可见性、URL、origin、上游、认证、网页未更新、clone、权限和 non-fast-forward。

### 验收

- 离线远程闭环通过：3 次本地提交、`origin/main` 上游、远程 `main` 与 clone HEAD 一致，网络和凭据使用均为零。
- 本课 34 条问法 Top 3 为 100%，2 条未知问题正确降级；其余正式课程与 13 个冻结样板回归保持通过。
- 正式迁移统计更新为：7 节已迁移、43 节待迁移、5 节样板已通过待迁移。
- 下一步迁移《开发环境》，继续在同一工作台记录解释器／编译器、PATH、依赖与版本，解决“我的电脑能运行但别人不能”的问题。

## 2026-07-17：正式 V2 迁移推进到《本地 Git 与 .gitignore》

### 已完成

- 将工程基础第六课从“五步 Git 命令任务”重写为“本地历史结果 → 工作区／暂存区／提交关系 → 状态符号 → 安装验证 → 初始化工作区 → 忽略生成文件 → 两次提交 → 状态排错 → 暂存快照 → 工程学习工作台 v0.6”。
- 不再另建与前课脱节的演示项目；直接为 `learning-workspace` 建立本地历史，同时在初始化前检查当前目录是否已经受上层仓库管理，降低误操作真实项目的风险。
- 使用项目级 `.gitignore` 排除日志、虚拟环境、缓存、本机配置和常见系统文件；增加 `git check-ignore -v`、`git status --short --ignored` 与已跟踪文件的解释，明确忽略规则不是保密或清除历史的工具。
- 新增隔离 Git 验证脚本，真实初始化 `main` 分支、配置仓库级身份、生成两次提交，并检查暂存文件、忽略匹配、提交顺序和未跟踪日志。
- 小码切换为 V2 语义知识库，共 15 张卡、30 条自然问法，覆盖安装、仓库根目录、三个区域、状态符号、忽略规则、身份、误暂存、已跟踪文件和项目交接。

### 验收

- 隔离 Git 示例通过：2 次提交、3 个受跟踪文件、1 个被忽略日志，`debug.log` 未进入索引。
- 本课 30 条问法 Top 1 / Top 3 均为 100%，2 条未知问题正确降级；其余正式课程与 13 个冻结样板回归保持通过。
- 正式迁移统计更新为：6 节已迁移、44 节待迁移、5 节样板已通过待迁移。
- 下一步迁移《GitHub 远程协作》，继续使用当前两次本地提交建立 `origin`、`push` 和 `clone` 闭环。

## 2026-07-17：正式 V2 迁移推进到《Markdown》

### 已完成

- 将工程基础第五课《Markdown》从五步语法任务改写为“源文本与预览对照 → 两者关系 → 五种常用结构 → 整理既有记录 → VS Code 侧边预览 → 换成真实内容 → 渲染排错 → 平台兼容 → 工程学习工作台 v0.5”。
- 延续上一课的 `notes/learning-log.md`，新增 `practice/README.md` 和整理前对照文件；标题、列表、链接、代码块和表格不再是孤立练习，而是共同服务同一份学习记录。
- 明确 CommonMark 基础语法与 GFM 表格扩展的区别，避免把 VS Code、GitHub 和其他 Markdown 渲染器说成完全一致。
- 增加无需 JavaScript 的源文本／阅读效果对照，移动端自动改为纵向单列；预览、编辑区和终端共同复核保存后的同一文件。
- 小码切换为 V2 语义知识库，共 13 张卡、26 条自然问法，覆盖渲染、保存、标题、列表、相对链接、代码围栏、表格、预览和兼容差异。

### 验收

- 本课 26 条问法 Top 1 / Top 3 均为 100%，2 条未知问题正确降级；其余正式课程与 13 个冻结样板回归保持通过。
- 正式迁移统计更新为：5 节已迁移、45 节待迁移、5 节样板已通过待迁移。
- 下一步迁移《本地 Git 与 .gitignore》，把工程学习工作台从手工备份推进到可追踪的本地版本历史。

## 2026-07-17：已验收 VS Code 样板进入正式课程

### 已完成

- 将工程基础第四课《编辑器：从安装 VS Code 开始》由五步任务结构迁移为“完成后的三个结果 → 界面关系 → 安装打开 → 打开工作区 → 保存复核 → 搜索与终端 → 常见问题恢复 → 工程学习工作台 v0.4”。
- 吸收批次 A 已验收样板的原创截图和自然化表达，并按正式课要求补齐 Windows、macOS、Linux 安装入口、工作区信任、适用版本、官方来源和下一课关系。
- 延续 `learning-workspace`，让编辑区、工作区搜索和内置终端同时找到学习者自己的独特关键词；下一课继续整理同一份 `learning-log.md`。
- 提取正式截图组件，支持深浅色、移动端单列、文字替代和无需 JavaScript 的完整操作路径。
- 小码切换为 V2 语义知识库，共 11 张卡、22 条自然问法，覆盖界面区域、安装、工作区、Restricted Mode、保存、搜索、终端和排错。

### 验收

- 本课 22 条问法 Top 1 / Top 3 均为 100%，2 条未知问题正确降级；其余正式课程与 13 个冻结样板回归保持通过。
- 正式迁移统计更新为：4 节已迁移、46 节待迁移、5 节样板已通过待迁移。
- 下一步迁移《Markdown》，继续在同一学习记录上完成结构化表达，不跳出工程基础连续路径。

## 2026-07-17：正式 V2 迁移推进到《终端与 Shell》

### 已完成

- 将工程基础第三课《终端与 Shell》重写为“真实读取结果 → 终端与 Shell 的关系 → 找到并打开终端 → 看懂命令 → 进入工作区 → 换位置读取文件 → 常见错误恢复 → 工程学习工作台 v0.3”。
- Windows 使用 PowerShell 完整命令，macOS 与 Linux 使用常见 Shell 命令；分别说明入口、提示符、当前目录和命令结果，避免把终端、Shell 和命令混为一谈。
- 延续 `learning-workspace`，从两个不同位置读取同一份 `notes/learning-log.md`，让相对路径、当前目录和文件系统在一次实际操作里连起来。
- 增加无需 JavaScript 的“人—终端—Shell—操作系统”关系图；补充复制提示符、路径错误、引号未闭合、同名目录和无输出等恢复方法。
- 小码切换为 V2 语义知识库，共 11 张卡、22 条自然问法，覆盖终端入口、命令结构、目录定位、文件读取、PowerShell 差异和命令排错。

### 验收

- 本课 22 条问法 Top 1 / Top 3 均为 100%，2 条未知问题正确降级；其余正式课程与 13 个冻结样板回归保持通过。
- 正式迁移统计更新为：3 节已迁移、46 节待迁移、6 节样板已通过待迁移。
- 下一步迁移《编辑器》，吸收已通过评审的 VS Code 样板，并继续使用同一个工程学习工作台。

## 2026-07-17：正式 V2 迁移推进到《文件系统》

### 已完成

- 将工程基础第二课《文件系统》从五步任务与定义堆叠，重写为“真实目录树 → 文件与目录 → 扩展名 → 路径起点 → 文件管理器复现 → 换文件判断 → 逐层排错 → 工程学习工作台 v0.2”。
- 延续上一课的 `learning-log.md`，形成 `learning-workspace/notes`、`practice`、`assets` 的稳定目录结构；下一课将直接从终端读取同一工作区。
- 增加无需 JavaScript 的目录树和路径关系图；移动端自动改为纵向布局，颜色不是唯一信息。
- Windows 11 文件资源管理器和 macOS Finder 的入口、创建目录与显示扩展名操作已按 Microsoft、Apple 官方文档核查。
- 小码切换为 V2 语义知识库，共 10 张卡、20 条自然问法，覆盖根目录、扩展名、相对路径、绝对路径、平台操作和路径排错。

### 验收

- 本课 20 条问法 Top 1 / Top 3 均为 100%，2 条未知问题正确降级。
- 正式迁移统计更新为：2 节已迁移、47 节待迁移、6 节样板已通过待迁移。
- 下一步迁移《终端与 Shell》，在刚建立的工作区上完成第一次命令行定位与读取。

## 2026-07-17：第一节正式 V2 课程完成迁移

### 已完成

- 将工程基础第一课《学习方法》从五步任务开场重写为“具体记录 → 学习循环 → 小目标例子 → 亲手复现 → 主动修改 → 模糊记录排错 → 工程学习工作台”的 V2 语义结构。
- 保留原课程 URL、标题、`engineering-foundation-01` 助教标识和下一课关系；课程内容不再使用 `be-task-route`、固定 `step-N` 或管理化表达。
- 小码知识库切换为 V2 `contexts` 协议，建立 8 张自然语言知识卡与 16 条固定问法；原始提问不进入本地存储。
- 助教全量测试改为依据迁移台账自动选择正式课程的 V1 或 V2 协议，不再假设 55 节课程全部处于 V1。

### 验收

- 第一课 16 条问法 Top 1 / Top 3 均为 100%，2 条未知问题正确降级；其余 54 节 V1 正式课程与 13 个冻结样板回归保持通过。
- 正式迁移统计更新为：1 节已迁移、48 节待迁移、6 节样板已通过待迁移。
- 下一步沿工程学习工作台继续迁移《文件系统》，不提前跳到零散课程。

## 2026-07-17：正式课程目录对齐 V2 架构

### 已完成

- 顶部“开始学习”课程树改为共同基座优先：工程基础 → Python 起步 → CS 起步；之后再显示 C++、Python 核心与工程化、算法与数据结构方向。
- C++ 五课在登记、课程地图和导航中拆分为起步 2 节、核心 3 节；Python 七课拆分为核心 5 节、工程化 2 节；CS 课程拆分为起步 4 节、共同算法基础 16 节、算法核心 6 节。
- 重写编程语言、C++、Python 核心课程说明，并修订 Python 起步和 CS 说明，取消“所有人必须 Python/C++ 双主修”的旧入口，改为共同基座后按方向选择。
- 扩展课程登记校验：55 个正式 URL 在导航中只能出现一次，共同基座顺序和七个公开层级必须存在，课程当前模块必须与迁移目标模块一致。

### 验收

- `validate_curriculum_v2.cjs` 通过：37 个模块、55 节课程、7 个迁移批次，C++ 起步 2 节与核心 3 节登记一致。
- `mkdocs build --strict` 通过；桌面课程树显示新的分组，390px 页面无横向溢出，长表格保持可读。
- 本轮只调整目录、课程组说明和前置口径，没有迁移正式单课正文或知识库；课程 URL 全部保持稳定。

## 2026-07-17：55 节正式课程 V2 迁移审计完成

### 已完成

- 新建 `site-src/data/curriculum/migration-v2.json`，逐课冻结目标模块、六级深度、角色、课型、项目线、迁移批次、前置课程、稳定 `tutor lesson_id` 和迁移状态。
- 将 55 节课程分为 7 个连续迁移批次：工程基础、Python 起步、CS 起步、Python 核心与工程化、C++ 起步与核心、共同算法基础、算法核心。
- 明确现有 C++ 前两课迁移后归入 C++ 起步，后三课归入 C++ 核心；现有 CS 前四课重写为真正的小白 CS 起步，URL 均保持不变。
- 识别 6 节已有通过评审的对应样板：VS Code、Python 变量、CS 数据表示、C++ 编译、RAII 和 BFS；其余 49 节按课型重新组织。
- 扩展 `scripts/validate_curriculum_v2.cjs`，校验 55 节完整覆盖、批次数量、目标层级、课型、项目线、前置顺序、稳定小码 ID 以及 V1/V2 状态一致性。
- 新增 `docs/11_course_migration_ledger.md`，作为迁移批次、重分层结论和状态规则的人类可读入口。

### 当前边界

- 本轮只完成逐课审计和迁移控制面，没有改写任何正式课程正文、知识库或 URL。
- 当前为 49 节“待迁移”、6 节“样板已通过待迁移”、0 节“已迁移”。
- 下一步进入第一批工程基础 10 节，先迁移连续课程组，再调整与该组事实一致的公开目录和导航。

## 2026-07-17：课程内容规范与生产 Skill V2 已固化

### 已完成

- 维护者确认十三个样板通过跨方向统一评审；共同语义骨架、八类课型差异、自然语言基线、功能型视觉和六条项目线正式采用。
- 将 `docs/08_content_standard.md` 改为 V2 正式规范，建立公共共同模板和八类课型差异模板。
- 小码正式检索与运行时支持 V1 `steps` 和 V2 `contexts`，分别使用独立本地存储键；统一样板站点通过薄适配器复用正式运行时。
- 助教校验改为读取课程架构登记，同时回归 55 节 V1 正式课程和 13 个 V2 冻结样板。
- 升级仓库内 `$become-engineer-course-authoring`，支持新建 V2、V1 迁移到 V2、维护 V1 三种模式，并同步个人安装版。

### 当前边界

- 本轮没有迁移正式课程，55 节课程的 URL、导航、正文和知识库保持不变。
- 批次 A、B、C 正文继续冻结；批次视觉与互动组件在首个正式迁移课程组中按需提取。
- 下一步选择一个连续课程组迁移，并在该组上组织四类用户试学。

## 2026-07-17：十三个样板跨方向统一评审待确认

### 已完成

- 新建 `reviews/course-content/unified-review/` 与 `mkdocs.samples-unified.yml`，将批次 A、B、C 的十三个冻结样板放入同一导航，统一评审站点与正式站点继续隔离。
- 完成八类课型覆盖矩阵、共同骨架与允许差异、六条连续项目线、小码 V2 候选协议、V1 兼容要求和缺口登记。
- 新增版本化样板登记清单，记录 13 个样板的课型、批次、项目线、运行依赖与来源状态；页面正文没有复制或修改。
- 合并评审阶段的小码加载逻辑为一份隔离候选运行时，复用三批原有知识库；本地只保存面板状态和提示展开层级。
- 新增统一校验器，十三页共 109 张知识卡、218 条固定问法全部通过检索门槛，八类课型与七类共同语义环节覆盖完整。
- 将三批脚本、校验与视觉重复项，批次 A／B 来源区块，以及正式 V1 `steps` 兼容列入下一阶段修订清单，本轮不回改已验收页面。

### 当前边界

- 统一评审技术交付已完成，状态为“待维护者确认”；确认前不进入规范和生产链固化。
- 现有 55 节正式课程、URL、导航、V1 助教、`docs/08_content_standard.md`、模板和课程生产 Skill 均保持不变。

## 2026-07-17：课程内容样板批次 C 已验收

### 本轮完成

- 用户确认批次 B 的内容与表达通过评审，五页样板冻结；批次 A、B 继续作为当前视觉和语言基线。
- 新建 `reviews/course-content/batch-c/`、`mkdocs.samples-c.yml` 和独立依赖，交付 Web、AI、LLM、Agent 四页样板。
- Web 将学习进度报告器接入 FastAPI；AI 建立 240 条教学合成记录的固定训练验证实验；LLM 建立严格 `LearningRequest`；Agent 只查询固定公开课程目录。
- LLM／Agent 默认离线运行，可选 DeepSeek 适配器只读本机环境变量；自动测试、页面源码、日志和构建不保存或使用 API Key。
- 四页建立 32 张语义知识卡和 64 条固定问法，继续保留自然提问、两层提示、来源锚点和未知问题降级。
- 用户确认四页内容、结构和语言风格通过评审；结构化输出完成一次真实 DeepSeek Schema 验收，Agent 完成一次真实 `get_course_summary` 只读工具调用。
- 真实调用验证发现 V4 模型不接受强制工具参数，最终保持 `tool_choice=auto`，通过更明确的系统提示要求先查目录；模型未提出单一工具调用时应用继续拒绝执行。
- DeepSeek 网络、限流和接口异常统一收敛为不包含请求与回复内容的简短错误类型，不再输出 SDK 完整堆栈。

### 验收结论与下一步

- 批次 C 已冻结但不属于正式开放课程；现有 55 节课程、正式导航、V1 助教、内容规范和 Skill 均未修改。
- 批次 A、B、C 已分别通过用户评审，下一步进入三批样板的跨方向统一评审，不直接迁移正式课程。

## 2026-07-17：课程内容样板批次 B 已验收

### 本轮完成

- 用户已确认批次 A 的内容、交互和自然化语言风格通过评审；四页样板冻结为后续批次的当前设计基线，但尚未写入正式内容规范与课程生产 Skill。
- 新建 `reviews/course-content/batch-b/`、`mkdocs.samples-b.yml` 和独立构建目录，交付 C++ 起步、C 起步、BFS、RAII、GPIO／中断五个样板。
- C++ 起步与 RAII 共同演进学习进度报告器；C 起步与 GPIO 共同演进设备事件记录器；BFS 继续连接现有双语言可追踪图遍历实验。
- GPIO 使用浏览器状态轨迹与可测试 C 假 HAL，不绑定开发板；页面明确区分程序逻辑仿真和必须在真机完成的电气、时序、并发与性能验证。
- 五页首稿直接采用批次 A 已验收的老师陪练口吻；静态校验禁止重新出现旧任务路线和高频管理化表达。
- 新建 43 张语义知识卡和 86 条固定问法；正式 V1 助教、55 节课程、正式导航、URL、内容规范和 Skill 均未修改。

### 验收结论

- 用户确认五页内容、系统图解、工具链、BFS 状态、RAII 生命周期、GPIO 仿真边界和语言表达通过评审。
- 批次 B 已冻结但仍只存在于独立样板站点，不属于正式开放课程；后续进入批次 C，不提前修改正式内容规范与 Skill。

## 2026-07-17：课程内容样板批次 A 已验收

### 本轮完成

- 在 `reviews/course-content/batch-a/` 建成 VS Code、Python 变量、CS 起步和学习进度报告器四个独立样板，统一采用“结果预览—心智模型—微型例子—引导复现—主动修改—排错—项目增量—分层深化”。
- 新增独立 `mkdocs.samples.yml` 与样板视觉层；正式 `mkdocs.yml` 仅用 `draft_docs` 排除样板，生产导航、搜索、课程 URL 和 55 节课程计数保持不变。
- VS Code 样板使用受控工作区制作的原创界面截图与编号标注；Python 样板提供按需加载的 Pyodide 0.29.4 Worker 运行器；CS 样板提供可键盘操作的扫描轨迹和无 JavaScript 静态表；阶段作品展示 `v0.1` 至 `v1.0` 的连续版本线。
- 为四个样板建立隔离的语义上下文助教原型，共 34 张审核知识卡、68 条固定问法；当前正式 V1 助教、知识库和检索测试未修改。

### 验证结果

- 四套样板知识库固定问法 Top 1／Top 3 均为 100%，未知问题全部进入不猜测降级。
- 6 个独立 Python 示例测试通过；浏览器运行器实际验证成功输出、语法错误和一键恢复；CS 扫描在第 3 次比较找到目标值。
- 正式站点与独立样板站点严格构建通过；桌面和 390px 手机端无页面横向溢出，手机端小码为全屏面板。

### 首轮评审反馈与文案修订

- 用户确认课程内容与整体体验方向良好，同时指出正文与小码回答仍有明显的 AI 式表达：管理术语偏多、标题过长、排比和固定句式过密。
- 保留四页结构、知识点、代码、交互与项目关系，统一把文案改为“老师陪练为主、简洁教材承载定义、少量作者判断点睛”。
- 同步修订评审首页、四页正文、34 张小码知识卡、推荐问题和界面提示；卡片 ID、上下文、来源、检索接口与正式 V1 助教均不改变。
- 文案不虚构作者经历，不使用 AI 文本检测器作为验收依据；最终仍以用户阅读感受、技术准确性和新手理解效果评审。

### 验收结论

- 用户确认自然化后的课程内容、交互体验和语言方向通过评审，可作为批次 B、C 的当前样板基线。
- 批次 A 仍不是正式课程，也不会提前替换 `docs/08_content_standard.md`、课程模板或课程生产 Skill；这些变更继续等待三批样板的跨方向统一评审。

## 2026-07-16：课程内容架构修订总计划

### 实施目标

- 将“理解驱动、小步实践、项目贯穿、分层深化”确立为待样板验证的目标内容架构。
- 把八类课型、13 个样板、项目体系、助教演进和 55 节课程迁移顺序写入唯一总计划。

### 本轮完成

- 新增 `docs/10_course_content_revision_plan.md`，明确三个样板批次、每阶段输入／产出／验收／停止条件和当前状态表。
- 同步项目总览、PRD、路线图、架构、验收、决策、进度和学习体验计划；现有任务优先契约明确为当前实现，不再作为所有未来课型的目标。
- 保留 `docs/08_content_standard.md`、现有模板、仓库与个人 Skill、小码 V1 协议、55 节课程正文和课程架构登记不变。

### 范围边界

- 本轮不制作样板、不创建视觉或浏览器运行组件、不修改导航、课程 URL、知识库或个性化规则。
- 规划中的 C、C++ 起步、CS 起步、Web、AI、LLM、Agent 和设备内容仍保持未建设状态。

### 下一步

- 下一轮只推进样板批次 A：VS Code、Python 变量、CS 起步和学习进度报告器阶段作品；完成评审后再决定是否进入批次 B。

## 2026-07-16：全课程架构总表与状态流程图 V2

### 实施目标

- 统一四类服务对象、共同基座、方向分流、AI／LLM／Agent 边界和设备交叉解锁口径。
- 将完整课程地图升级为唯一公开课程控制面，保留 55 节课程 URL、正文、助教和顶部课程树。

### 实施范围

- 建立 37 个稳定模块和 55 节课程的版本化登记，记录六级深度、四类角色、前置、解锁、画像规则、内容状态、衔接状态和页面锚点。
- 使用无 JavaScript 依赖的响应式 HTML/CSS 流程展示共同基座、七个方向和三类跨线汇合；手机端改为纵向分组。
- 开始学习页收缩为四类入口、共同基座和方向摘要；总学习地图收缩为架构摘要并回链权威地图。

### 验收结果

- 课程登记静态校验已通过：37 个模块、55 节课程、55 个唯一 URL，登记标题与课程一级标题一致，权威地图逐课链接完整。
- C++ 核心 5 节和 CS 26 节明确标为“已开放待重分层”；C++ 起步、CS 起步缺口及所有规划模块均明确显示未建设或等待前置。
- 个性化 V1 和 55 课助教回归通过：四类画像规则不变，478 张卡、952 条固定问法全部达到既有命中与降级门槛。
- `mkdocs build --strict` 通过；101 个 HTML 页面中的 14875 个内部链接与 5189 个锚点有效，`git diff --check` 通过。
- 1440×900 桌面端流程按三列分支显示；390×844 手机端为单列流程和字段化阶段卡片，浅色、深色、长标题、锚点跳转和键盘焦点均无横向溢出。

### 下一步

- 单独规划 C++ 起步、CS 起步与首个可验证方向项目；地图验收前不迁移顶部课程树和测评逻辑。

## 2026-07-16：CS 并查集、Kruskal 与 Lazy Prim 三课

### 实施目标

- 使用 `$become-engineer-course-authoring` 建设 `cs-core-24` 至 `cs-core-26`，完成集合连通性与最小生成森林基础闭环。
- 新建双语言“可追踪生成森林实验”，完整保留已验收 V2.8 与既有七个 CS 实验的代码及输出契约。
- 三课各建设 6 步任务路线、9 张审核卡与 18 条固定问法；目标规模为 55 节正式课程、478 张卡、952 条问法。

### 实施范围

- DSU 使用迭代完整路径压缩和按大小合并；同大小时编号较小的根保留代表身份，操作轨迹精确记录访问与真实父值修改。
- Kruskal 对简单无向图按 `(weight,u,v)` 排序，用 DSU 拒绝成环边；断开图返回森林，单树接口在断开图上安全失败。
- Lazy Prim 从最小未访问顶点启动每个分量，使用 `(weight,u,v)` 最小前沿并跳过两端均已访问的过期边。

### 验收结果

- Python 10 项 unittest 与 6 个源码/测试文件的 strict mypy 检查通过；editable 安装成功，wheel 在干净临时环境安装并从项目目录外运行。
- C++20 Release 严格警告构建与 CTest 通过；Python 与 C++ 的 `dsu`、`kruskal`、`prim` 三种输出逐字一致，未知模式均只写标准错误并返回 2。
- 数组、线性结构、哈希、查找排序、树遍历、图遍历、优先队列最短路及新生成森林共八个实验的 Python 测试和 CTest 全部回归通过。
- 55 课、478 张卡、952 条固定问法全部通过；新增三课 Top 1 与 Top 3 均为 100%，未知问题正确降级，个性化规则通过。
- `mkdocs build --strict` 与 `git diff --check` 通过；101 个 HTML 页面中的 14864 个内部链接与 5221 个锚点全部有效。
- 1440×900 与 390×844 真实渲染确认三课均有 6 步路线、助教、代码与图表，无横向溢出；浅色模式与键盘激活通过，静态 HTML 保留完整正文用于禁用 JavaScript 降级。
- 生成站点、wheel、egg-info、虚拟环境、缓存与 CMake 构建目录均未进入 Git。

### 下一步

- V2.9 已完成。下一阶段进入计算机运行基础；有向图、负权最短路、网络流与正式机考继续保持后续范围。

## 2026-07-16：CS 最小堆、稳定优先队列与 Dijkstra 三课

### 实施目标

- 使用 `$become-engineer-course-authoring` 建设 `cs-core-21` 至 `cs-core-23`，完成堆不变量、稳定优先队列和非负权最短路基础闭环。
- 新建双语言“可追踪优先队列与最短路实验”，保持既有六个 CS 实验及其输出契约不变。
- 三课各建设 6 步任务路线、9 张审核卡与 18 条固定问法；目标规模为 52 节正式课程、451 张卡、898 条问法。

### 实施范围

- 最小堆使用零基隐式完全二叉树，逐操作记录元素比较和真实交换；空堆读取与删除不得改变状态。
- 稳定优先队列按 `(priority, sequence)` 排序，不把内部堆数组描述为全局有序，也不依赖标签破除平局。
- Dijkstra 使用非负权简单无向图、严格松弛和懒惰重复项；过期距离项在扫描邻接表前跳过，并检查 64 位距离加法溢出。

### 验收结果

- Python 8 项 unittest 与 7 个源码/测试文件的 strict mypy 检查通过；editable 安装成功，wheel 在干净临时环境安装并从项目目录外运行。
- C++20 Release 严格警告构建与 CTest 通过；Python 与 C++ 的 `heap`、`queue`、`dijkstra` 三种输出逐字一致，未知模式均只写标准错误并返回 2。
- 数组实验 21 项、线性结构实验 10 项、哈希实验 9 项、查找排序实验 16 项、树遍历实验 15 项、图遍历实验 13 项 Python 测试及各自 CTest 全部回归通过。
- 52 课、451 张卡、898 条固定问法全部通过；新增三课 Top 1 与 Top 3 均为 100%，未知问题正确降级，个性化规则通过。
- `mkdocs build --strict` 与 `git diff --check` 通过；97 个 HTML 页面中的 13981 个内部链接与 5105 个锚点全部有效。
- 1440×900 与 390×844 真实渲染确认三课均有 6 步路线、助教入口、代码与 Mermaid，页面无横向溢出；手机端助教面板可打开，静态 HTML 保留完整正文用于禁用 JavaScript 降级。
- 生成站点、wheel、egg-info、虚拟环境、缓存与 CMake 构建目录均未进入 Git。

### 下一步

- V2.8 已完成。下一批评估最小生成树与并查集；负权最短路、有向图算法和正式机考继续保持后续范围。

## 2026-07-16：CS 一般图表示、BFS 与 DFS 三课

### 实施目标

- 使用 `$become-engineer-course-authoring` 建设 `cs-core-18` 至 `cs-core-20`，完成简单无向图表示、无权 BFS 和连通分量 DFS 的基础闭环。
- 新建双语言“可追踪图遍历实验”，保持数组、线性结构、哈希、查找排序、树遍历实验及其既有输出不变。
- 三课各建设 6 步任务路线、9 张审核卡与 18 条固定问法；目标规模为 49 节正式课程、424 张卡、844 条问法。

### 实施范围

- 图构造复制并规范化无向边，顶点固定为 `0..n-1`，拒绝越界、自环和重复边；邻接表始终按顶点编号排序。
- BFS 在入队时标记，返回访问顺序、距离、父节点、访问数、边检查数和最大前沿；最短路径只承诺简单无向无权图。
- DFS 通过全图最小未访问顶点启动新分量，使用父边区分无向回边，并返回确定性分量、首次环边和分量标签。

### 验收结果

- Python 13 项 unittest 与 9 个文件的 strict mypy 检查通过；使用仓库开发依赖完成 editable 安装，wheel 在干净临时环境安装，并从项目目录外运行。
- C++20 Release 严格警告构建与 CTest 通过；Python 与 C++ 的 `graph`、`bfs`、`dfs` 三种输出逐字一致，未知模式均只写标准错误并返回 2。
- 数组实验 21 项、线性结构实验 10 项、哈希实验 9 项、查找排序实验 16 项、树遍历实验 15 项 Python 测试及各自 CTest 全部回归通过，所有旧模式双语言输出保持一致。
- 49 课、424 张卡、844 条固定问法全部通过；新增三课 Top 1 与 Top 3 均为 100%，未知问题正确降级，个性化规则通过。
- `mkdocs build --strict` 与 `git diff --check` 通过；93 个 HTML 页面中的 13137 个内部链接与 4995 个锚点全部有效。
- 桌面与 390px 真实渲染确认六步路线、表格、代码块、主题样式、助教入口与无横向溢出；助教面板可打开，静态 HTML 保留完整正文和 Mermaid 源码用于禁用 JavaScript 降级。
- 生成站点、wheel、egg-info、虚拟环境、缓存与 CMake 构建目录均未进入 Git。

### 下一步

- V2.7 已完成。下一批评估带权最短路与优先队列，或进入计算机运行基础；有向图、拓扑排序、强连通分量与正式机考继续保持后续范围。

## 2026-07-16：CS 二叉树、递归 DFS 与显式前沿三课

### 实施目标

- 使用 `$become-engineer-course-authoring` 建设 `cs-core-15` 至 `cs-core-17`，完成树的表示、递归遍历和迭代前沿基础闭环。
- 新建双语言“可追踪树与遍历实验”，保持数组、线性结构、哈希、查找排序实验及其既有输出不变。
- 三课各建设 6 步任务路线、9 张审核卡与 18 条固定问法；目标规模为 46 节正式课程、397 张卡、790 条问法。

### 实施范围

- 二叉树输入使用零基层序槽位并复制规范化；非空输入拒绝空根和孤儿节点，链接节点在 C++ 中由 `std::unique_ptr` 单一拥有。
- 递归前中后序遍历记录访问次数和最深节点深度，使用显式教学深度上限执行安全失败，不修改 Python 解释器递归限制。
- 迭代 DFS 固定右后左先的压栈策略，BFS 固定左后右的入队策略；显式记录最大前沿和层级边界。

### 验收结果

- Python 15 项 unittest 与 9 个文件的 strict mypy 检查通过；editable 与 wheel 均在临时环境从项目目录外运行，未知模式只写标准错误并返回 2。
- C++20 Release 严格警告构建与 CTest 通过；C++ 树对象的 move-only 静态断言通过，Python 与 C++ 三种输出逐字一致。
- 数组实验 21 项 Python 测试与 CTest、线性结构实验 10 项 Python 测试与 CTest、哈希实验 9 项 Python 测试与 CTest、查找排序实验 16 项 Python 测试与 CTest 全部回归通过。
- 46 课、397 张卡、790 条固定问法全部通过；新增三课 Top 1 与 Top 3 均为 100%，未知问题正确降级，个性化规则通过。
- `mkdocs build --strict` 与 `git diff --check` 通过；89 个 HTML 页面中的 12307 个内部链接与 4870 个锚点全部有效。
- 桌面与 390px 真实渲染确认六步路线、主题、助教面板、代码与无横向溢出；构建 HTML 保留完整正文和 Mermaid 源码，禁用 JavaScript 不阻塞学习内容。
- 生成站点、wheel、egg-info、虚拟环境与 CMake 构建目录均未进入 Git。

### 下一步

- V2.6 验收后进入一般图的表示与遍历；搜索树、平衡树、堆、最短路、回溯、动态规划和正式机考继续保持在后续范围。

## 2026-07-16：CS 有序查找、基础排序与迭代归并三课

### 实施目标

- 使用 `$become-engineer-course-authoring` 建设 `cs-core-12` 至 `cs-core-14`，完成有序查找、比较排序稳定性和迭代归并的基础闭环。
- 新建双语言“可追踪查找与排序实验”，保持数组、线性结构、哈希实验及其既有输出不变。
- 三课各建设 6 步任务路线、9 张审核卡与 18 条固定问法；目标规模为 43 节正式课程、370 张卡、736 条问法。

### 实施范围

- `SortedValues` 在构造时复制并验证非递减顺序，后续查找计数不包含一次性验证成本；二分区间固定为 `[left, right)`。
- 插入排序只在严格越过时移动相等键；选择排序保留标准交换反例，使用带标签元素验证稳定性。
- 归并排序采用自底向上宽度 1、2、4 的迭代流程，相等键固定先取左段；比较和写入次数均由教学实现确定。

### 验收结果

- Python 16 项 unittest 与 8 个文件的 strict mypy 检查通过；editable 与 wheel 均在干净临时环境从项目目录外运行，未知模式只写标准错误并返回 2。
- C++20 Release 严格警告构建与 CTest 通过；Python 与 C++ 的 `search`、`elementary`、`merge` 三种输出逐字一致。
- 数组实验 21 项 Python 测试与 CTest、线性结构实验 10 项 Python 测试与 CTest、哈希实验 9 项 Python 测试与 CTest 全部回归通过。
- 43 课、370 张卡、736 条固定问法全部通过；新增三课 Top 1 与 Top 3 均为 100%，未知问题正确降级，个性化规则通过。
- `mkdocs build --strict` 与 `git diff --check` 通过；85 个 HTML 页面中的 11492 个内部链接与 4727 个锚点全部有效。
- 桌面与 390px 真实渲染确认六步路线、表格、代码、Mermaid、助教、深浅色与无页面横向溢出；静态 HTML 保留完整正文和图源码，禁用 JavaScript 不阻塞学习内容。
- 生成站点、wheel、egg-info、虚拟环境与 CMake 构建目录均未进入 Git。

### 下一步

- V2.5 验收后进入树与递归；快速排序、堆排序、非比较排序、外部排序、并行排序和正式机考继续保持在后续范围。

## 2026-07-16：CS 哈希、分离链接与集合应用三课

### 实施目标

- 使用 `$become-engineer-course-authoring` 建设 `cs-core-09` 至 `cs-core-11`，完成哈希函数、分离链接哈希表和集合应用的基础闭环。
- 新建双语言“可追踪哈希实验”，保持数组实验、线性结构实验及其既有输出不变。
- 三课各建设 6 步任务路线、9 张审核卡与 18 条固定问法；目标规模为 40 节正式课程、343 张卡、682 条问法。

### 实施范围

- 教学哈希固定为非负整数键对桶数取模，只作为确定性实验，不代表 Python 或 C++ 标准容器的实际哈希算法。
- 哈希表固定使用分离链接、1.0 最大负载因子和倍增扩容；比较次数、搬移数量及桶链顺序均可复现。
- 集合应用显式区分判重状态和输出顺序；频次按键排序，保序去重保留第一次出现位置。

### 验收结果

- Python 9 项 unittest 与 6 个文件的 strict mypy 检查通过；editable 与 wheel 安装均已从项目目录外运行，未知模式只写标准错误并返回 2。
- C++20 Release 严格警告构建与 CTest 通过；Python 与 C++ 的 `hash`、`table`、`applications` 三种输出逐字一致。
- 数组实验 21 项 Python 测试与 CTest、线性结构实验 10 项 Python 测试与 CTest 全部回归通过，既有成果线未改变。
- 40 课、343 张卡、682 条固定问法全部通过，新增三课 Top 1 与 Top 3 均为 100%，未知问题正确降级；个性化规则与 `mkdocs build --strict` 通过，81 个 HTML 页面中的 10698 个内部链接与 4575 个锚点全部有效。
- 三课生成 HTML 均保留 6 步任务、助教挂载点、Mermaid 源码和代码块，深浅色、焦点与横向滚动样式已进入构建产物；生成站点、wheel、egg-info 与构建目录均由 Git 忽略。
- 用户已完成桌面与 390px 浏览器视觉验收并确认通过。

### 下一步

- 进入查找与排序课程；开放寻址、哈希攻击、LRU、树、递归和正式机考训练继续保持在后续范围。

## 2026-07-16：CS 单链表、栈与队列三课

### 已完成

- 使用 `$become-engineer-course-authoring` 建设 `cs-core-06` 至 `cs-core-08`，完成节点所有权、LIFO 和 FIFO 的线性结构闭环。
- 新建双语言“可追踪线性结构实验”，不修改已冻结的数组实验与既有课程输出。
- 三课各建设 6 步任务路线、9 张审核卡与 18 条固定问法；正式课程已扩展为 37 节、316 张卡、628 条问法。

### 实施范围

- Python 使用私有节点实现单链表、链式栈和链式队列；C++ 使用 `std::unique_ptr` 所有权链与队列非拥有尾指针。
- 固定 `linked`、`stack`、`queue` 三种模式，无参数等价于 `linked`，未知模式返回 2。
- 链表迁移任务为 `remove_first`，栈迁移任务为 `drain_stack`，队列迁移任务为 `serve_until_empty`。

### 验收结果

- Python strict mypy、10 项 unittest、editable 与 wheel 安装均通过；两种安装方式都已从项目目录外运行模块入口。
- C++20 Release 严格警告构建与 CTest 通过；三个公开容器均验证不可复制、可移动和移动后源对象为空。
- Python 与 C++ 的 `linked`、`stack`、`queue` 输出逐字一致；未知模式只写标准错误并返回 2。
- 37 课、316 张卡、628 条固定问法全部通过，逐课 Top 3 命中率均为 100%，未知问题正确降级；个性化规则和 `mkdocs build --strict` 通过，77 个 HTML 页面中的 9929 个内部链接与 4417 个锚点全部有效。
- 三课在桌面和 390px 视口均有 6 步任务、助教入口、表格、代码块与 Mermaid 源码，无页面级横向溢出；助教面板可打开，深浅色与焦点样式沿用已验收基线，静态 HTML 保留完整正文。

### 下一步

- 进入哈希与集合课程；双向链表、循环链表、环形缓冲区、优先队列、排序和正式机考训练继续保持在后续范围。

## 2026-07-16：CS 字符串、二维网格与动态数组三课

### 已完成

- 使用 `$become-engineer-course-authoring` 建设 `cs-core-03` 至 `cs-core-05`，完成进入链表前的序列表示闭环。
- 延续 Python/C++“可追踪数组实验”，新增严格 UTF-8 扫描、行优先二维网格和确定性容量增长模拟；默认报告保持不变。
- 三课各建设 6 步任务路线、9 张审核卡与 18 条固定问法；正式课程已扩展为 34 节、289 张卡、574 条问法。
- CS 课程说明、MkDocs 导航、课程地图、默认路线、个性化目录、阶段作品 README 和前后课链接均已同步，序列表示五课形成连续成果线。

### 验证结果

- Python 21 项 unittest 与 6 个文件的 strict mypy 检查通过；可编辑安装和干净虚拟环境 wheel 安装均能从项目目录外运行。C++20 Release 严格警告构建与 CTest 1/1 通过。
- Python 与 C++ 的 `baseline`、`text`、`grid`、`capacity` 输出分别逐字一致；无参数继续等于 `baseline`，未知模式只写 `stderr` 并返回 2。UTF-8、网格与容量边界均有确定性测试。
- 34 课、289 张卡、574 条固定问法全部通过，逐课 Top 3 命中率均为 100%，未知问题正确降级；个性化规则与 `mkdocs build --strict` 通过，73 个 HTML 页面中的 9174 个内部链接与 4245 个锚点全部有效。
- 三课在 1280px 与 390px 视口均有 6 步任务、助教、表格和 Mermaid，无页面级横向溢出或控制台错误；深浅色与焦点样式已加载，禁用 JavaScript 时构建 HTML 仍保留全部任务正文和 Mermaid 源码。

### 下一步

- 进入链表、栈和队列课程，继续沿用双语言确定性操作追踪；哈希、排序和正式机考训练仍不提前混入下一批。

## 2026-07-16：CS 序列、数组边界与复杂度首批双课

### 已完成

- 使用 `$become-engineer-course-authoring` 建设 `cs-core-01` 与 `cs-core-02`，以两节连续课程启动 CS 必修主干。
- 新建 Python/C++“可追踪数组实验”，用相同固定数据、检查式访问、复制修改、线性查找和增长表形成连续成果线。
- 两课各建设 6 步任务路线、9 张审核卡与 18 条固定问法；正式课程已扩展为 31 节、262 张卡、520 条问法。
- CS 课程说明、课程地图、默认路线、个性化目录、MkDocs 导航和 Python 第 7 课下一课入口已同步；CS 阶段由“规划中”进入“建设中”。

### 验证结果

- Python 11 项 unittest 与 6 个文件的 strict mypy 检查通过；C++20 Release 严格警告构建与 CTest 1/1 通过。Release 验收同时将会被 `NDEBUG` 移除的标准 `assert` 改为始终生效的显式测试失败。
- 从阶段作品目录外调用 Python 模块入口与 C++ 可执行程序均成功，两份标准输出逐字一致；负索引、空序列、`index == size`、副本不变、首个匹配和确定性操作次数均有测试证据。
- 31 课、262 张卡、520 条固定问法全部通过，逐课 Top 3 命中率均为 100%，未知问题正确降级；四类个性化规则、`mkdocs build --strict` 和 `git diff --check` 通过，70 个 HTML 页面的 8610 个内部链接与 4095 个锚点全部有效。
- 两课在桌面和 390px 视口均有 6 步任务、助教入口、Mermaid 源码、代码块与表格，无页面横向溢出或控制台错误；深浅色控件与键盘焦点样式进入构建产物，禁用 JavaScript 时静态 HTML 仍保留完整正文和流程图源码。

### 下一步

- 继续建设 CS 必修主干的字符串、二维数组、动态数组、链表、栈队列与排序课程；正式机考训练仍按独立求职训练包推进。

## 2026-07-16：设备系统与边缘智能专业路线接入

### 已完成

- 保持六条默认主线和 29 节正式课程不变，新增“设备系统与边缘智能”跨主线专业领域总览。
- 建立 C++ 与 CS 解锁设备共同基础、MCU/RTOS 与嵌入式 Linux/BSP 双平台并行、工业控制并列的能力架构。
- 明确 AI 推荐深化、强化学习、其他 AI 专业选修和 LLM/Agent 保持独立；边缘智能要求 AI 基础与设备平台能力，智能控制实验才要求强化学习。
- 建立一级仿真实践、二级真机模块、三级 BSP／边缘智能高级三个能力出口，并固定“仿真入门、真机毕业”口径。
- 将匿名 Linux/BSP 岗位描述仅作为能力校准样本，映射驱动、BSP、外设、性能、文档测试和视频图像能力，不推断市场频率。

### 范围边界

- 本轮未采集嵌入式素材、未扩充招聘样本、未选择开发板，也未创建正式课程、项目目录或小码知识库。
- 设备路线暂不进入“开始学习”课程树和个性化测评；首批课程开放后再设计方向入口。

### 验证结果

- `mkdocs build --strict` 通过；67 个 HTML 页面中的 8091 个内部链接与 3970 个锚点全部有效，`git diff --check` 通过。
- 29 课助教知识库回归和四类个性化路线规则测试通过，新增规划页未改变正式课程与测评逻辑。
- 设备路线依赖图改为不依赖运行时 JavaScript 的响应式流程图；桌面与 390px 手机端、深浅色、长标题和三列表转单列布局均通过人工检查。

### 下一步

- 后续单独规划 MCU/RTOS、Linux/BSP、外设驱动、工业通信和端侧推理素材与参考硬件，不依赖现有 AI 笔记拼接课程。

## 2026-07-15：求职参考素材 V2

### 已完成

- 冻结 V1 并建立独立 V2：23 个来源、18 个来源族、320 条机考信号和 280 条技术面试／项目追问信号。
- 使用 Codeforces 公开题目元数据、AtCoder 题目与难度数据和 GitHub 许可证元信息完成真实刷新；AtCoder 固定 1100ms 最小间隔，不读取用户提交数据。
- 建立 14 个主能力族、190 个聚合主题和三层证据解释；74 个主题满足至少三个独立来源族和 A 级来源的高频参考门槛。
- 完成 29 节正式课程和 CS、Web、AI、LLM/Agent 规划阶段的语义映射，修正了按大类标签机械轮转细主题的问题。
- 生成 48 个原创题蓝图、8 道完整机考题和 8 道完整技术面试题；外部题面、答案结构、题解代码和企业名称均未复用。
- 扩展 `$become-engineer-course-authoring`：新增 V2 脱敏查询脚本和原创转化规则，raw 素材继续保持不可见。

### 验证结果

- V2 校验已通过：600 条信号、23 个来源、18 个来源族、29 节课程、48 个蓝图、16 道完整题，受限正文和个人信息计数均为 0。
- V1 冻结回归、仓库与个人 Skill 结构校验、29 课助教检索、四类个性化规则和 `mkdocs build --strict` 均通过。
- 生成站点 66 个 HTML 页面的 8001 个内部链接与 3948 个锚点通过检查；`git diff --check` 通过，V2 素材与来源目录保持 ignored 且未被 Git 跟踪。

### 下一步

- 先人工评审 16 道完整题的歧义、难度和项目叙事，再决定哪些进入 CS 推荐深化或求职训练包。
- 根据 V2.1 缺口清单补充经授权的国内岗位专项来源，不因追求数量放宽版权与隐私边界。

## 2026-07-15：求职参考素材与原创转化 V1

### 已完成

- 建立“来源登记 → 本地参考信号 → 能力覆盖矩阵 → 原创题目草稿”的本地生产链。
- 将 12 个来源分为自动采集与稀疏快照、人工浏览与元数据登记两级；登录、付费、验证码、反爬、企业内部和个人信息内容明确禁止采集。
- 通过 Codeforces 官方 API 读取 4 条高阶算法元数据；对 JavaGuide、System Design Primer 和 Front End Interview Handbook 完成浅克隆、稀疏检出、许可证与提交版本登记。
- 完成 30 条机考信号和 30 条技术面试信号；受限平台不保存完整题面、答案、题解或用户信息。
- 生成能力覆盖矩阵、人工复核队列、课程生产结构化导出和 8 道原创转化草稿。
- 原创草稿使用本站学习报告器、课程依赖、审计任务、个性化画像和 Git 排错等新场景，记录独立约束、测试证据和原创变化，不标注企业真题。

### 下一步

- 邀请目标学习者试做 4 道机考草稿并回答 4 道项目证据题，记录歧义、耗时、提示需求和中断点。
- 通过试做后，再把合适题目挂到 CS 推荐深化或求职训练包，不直接把本地素材目录接入公开站点。
- 根据覆盖报告决定下一批优先扩展 AI 应用、后端或 C++ 专项，而不是一次性扩充全部方向。

## 2026-07-15：Python 包、CLI、配置与日志工程化收口

### 已完成

- 使用 `$become-engineer-course-authoring` 新增 Python 核心第 6、7 课，把多模块报告器迁移为可安装的 `src` 布局包，并提供 `study-progress` 与 `python -m study_progress_reporter` 两个等价入口。
- 第 6 课完成包结构、`pyproject.toml`、editable/wheel 安装、`argparse` 子命令和导入失败实验；第 7 课完成显式 TOML 配置、优先级、命名日志、标准流与退出码契约。
- 两课各建设 6 步任务路线、9 张审核卡和 18 条固定问法；正式课程已扩展到 29 节、244 张卡、484 条问法。

### 验证结果

- Python 30 项 unittest 与 13 个文件的 strict mypy 检查通过；默认报告保持总体进度 `87.1%`，并与 C++ 标准输出逐字一致。
- editable 安装与干净虚拟环境 wheel 安装均可从项目目录外调用控制台入口和模块入口；配置、优先级、日志分流、退出码与旧审计保护均有自动测试。
- C++ CTest 1/1、29 课知识库 484 条固定问法、四类个性化规则、MkDocs 严格构建和 Git 差异检查全部通过。
- 两课桌面与 390px 手机端无横向溢出，Mermaid、深浅色和助教面板正常；助教按钮可通过键盘 Enter 打开，静态 HTML 在禁用 JavaScript 时仍保留完整课程任务与正文。

### 下一步

- 按新版总课程表进入四类路线纵向验证和 CS 共同主干；网络、并发、数据库、Web、PyPI 发布与第三方 CLI 框架继续保持在本轮范围之外。

## 2026-07-15：小码入学测评与四类个性化路线 V1

### 已完成

- 首页新增可跳过的小码入学测评：先确认兴趣学习或求职准备，再用 8 道情境题判断工程工具、编程基础、算法与 CS、项目工程四个维度，不使用不可解释的单一总分。
- 建立“从零／已有基础 × 兴趣／求职”四类主画像、静态课程目录和确定性路线规则；结果确认后才改变“开始学习”、桌面课程树和手机抽屉，完整路线、搜索和直接 URL 继续可用。
- 未显示内容区分“测评认为已掌握、与当前目标不直接相关、前置尚未满足、内容尚未建设”，支持逐项加入与完整路线切换；浏览器只保存画像摘要、确认状态、导航模式和手动加入项，不保存原始回答。
- 工程基础扩展为 10 课：终端补齐 Windows/macOS 的寻找与首次验证，编辑器明确推荐 VS Code，本地 Git 增加安装、身份配置和 `.gitignore`，并新增 GitHub 远程协作课。
- 建立首个求职训练纵向样板：6 道原创分级 Python 机考题、45 分钟固定模拟，以及学习进度报告器的证据问答与口头表达结构；不冒充具体企业真题。
- 重规划完整课程地图：四类画像共用一张总课程表，以基础课程、新手操作包、深化挑战包和求职训练包组合路线；明确 8 个阶段的课程资产、连续成果、建设状态与后续顺序。
- 小码课程知识库扩展到 27 节、226 张审核卡和 448 条固定问法；个性化四类规则、27 课助教检索和 MkDocs 严格构建全部通过。
- 桌面端完成一次“已有基础 · 求职准备”全流程；390px 手机端复核测评结果、推荐按钮和全屏未显示内容面板，并修复按钮对比度与弹窗头部挤压。

### 待真实学习者验证

- 邀请至少 12 名目标用户，四类画像各不少于 3 人，记录判断认可度、首次起步、补修与路线调整结果。
- 继续验证“首次跳过后不打扰”、存储不可用、脚本或配置失败、深色模式、完整键盘链与禁用 JavaScript 降级；这些项目在验收清单中保持未完成状态。

## 2026-07-15：Python 装饰器、闭包与自定义上下文管理器

### 已完成

- 使用 `$become-engineer-course-authoring` 新增 Python 核心第 5 课，以五步任务完成基线、函数包装、类型保持、受控失败和分阶段文件提交。
- 双语言阶段作品新增 `trace_calls(event_sink)`，使用 `ParamSpec`、`TypeVar` 和 `wraps` 保持原调用契约；事件由调用者提供的 sink 接收，不进入主报告。
- 新增 `staged_output_path()` 生成器式上下文管理器；审计成功后才替换正式文件，块内失败时保留旧内容并清理临时文件。
- Python strict 类型检查和 16 项 unittest 通过；小码同学扩展到第 26 节课程，新增 9 张审核卡和 18 条固定问法。
- 修复课程生产 Skill 缺失的任务驱动正文模板，仓库版本继续作为唯一维护源。

### 下一步

- 建设 Python 包结构、CLI、配置与日志，把当前多模块报告器推进为最小工程化工具。
- 真实学习者试学时重点记录闭包、装饰时机、异常传播与 `yield` 前后阶段的提示层级。

## 2026-07-15：Python 数据模型、数据类与上下文管理

### 已完成

- 使用 `$become-engineer-course-authoring` 正式新增 Python 核心第 4 课，以五步任务完成字典基线、数据类迁移、对象方法、可变默认值失败和 `with` 审计验收。
- 双语言阶段作品的 Python 实现从 `TypedDict` 迁移为 `@dataclass`；加入进度、状态、复制和增加完成小时对象行为，保留惰性迭代与主报告文本。
- Python 新增与 C++ 相同格式的审计快照，成功和缺失父目录失败均有测试；mypy strict 和 13 个 unittest 通过。
- 小码同学扩展到第 25 节课程：新增 9 张审核知识卡、18 条固定问法，全部问法进入确定性检索测试。

### 下一步

- 建设 Python“装饰器与自定义上下文管理器”，继续解释函数包装、`contextmanager()` 和资源边界。
- 真实学习者试学时重点记录 `TypedDict`/数据类边界、浅复制和 `with` 错误处理的提示层级。

## 2026-07-15：课程生产 Skill 与 C++ 对象资源样板

### 已完成

- 新建仓库内 `$become-engineer-course-authoring` Skill，并同步安装到个人 Codex Skills；该 Skill 固化新课/重构两种模式、5–7 步任务路线、小码知识库、检索测试与正式站点接入检查。
- 正式新增 C++ 第五课“对象、引用、指针、生命周期与 RAII”，以双语言学习进度报告器为连续成果完成任务驱动样板。
- C++ 阶段作品新增按引用更新、可空非拥有查找和 `std::ofstream` 审计快照；成功与无法打开文件两条路径均由 CTest 验证，主报告标准输出保持不变。
- 小码同学扩展到第 24 节课程：新增 9 张审核知识卡、18 条固定问法，并继续使用确定性检索与未知问题降级。

### 下一步

- 让真实学习者试学本课，重点记录按值/引用混淆、空指针检查和文件失败路径的提示层级。
- 后续 Python 数据模型、上下文管理和资源边界课程落地后，再与本课形成下一组双语言能力对照。

## 2026-07-15：全课程任务驱动与知识库助教推广

### 已完成

- 已将工程基础 9 节、Python 起步 7 节、C++ 核心 5 节和 Python 核心 3 节共 24 节正式课程改造成 5–7 步任务路线；既有学习工作区、学习进度报告器和双语言学习报告器继续作为连续成果线。
- 小码同学已覆盖全部 24 节正式课程；每节均具备稳定课程标识、任务步骤锚点、课程专属知识库、分层提示、迁移验收和下一步入口。
- 新增 23 份知识库和固定测试集，加上样板课共 198 张知识卡、394 条固定问法；逐课 Top 1、Top 3 均为 100%，未知问题均正确降级。
- 知识库校验已改为自动发现正式课程，并校验 Markdown 挂载点、5–7 个任务步骤、来源锚点、卡片数量、问法数量和检索结果。
- `mkdocs build --strict`、JavaScript 语法检查、全量知识库测试、生成页挂载数量、资源 HTTP 访问和 `git diff --check` 均通过。

### 待真实学习者与视觉复核

- 继续邀请至少 5 名目标用户完成首次输出、核心任务、故障修复和迁移任务；用完成率、提示层级、无匹配问题与中断位置判断教学效果。
- 在实际桌面和手机浏览器复核各课程任务卡、对话面板、键盘焦点、深浅色、减少动画、资源失败与禁用 JavaScript 降级；不将自动构建结果替代为视觉验收。

## 2026-07-15：任务驱动课程与知识库像素助教 V1

### 已确认

- 目标用户收窄为希望借助 AI 学习编程、尚未形成系统工程能力的大学生与转行新手。
- 课程采用任务先行、即时反馈、按需讲解、主动修改、排错和迁移验收闭环。
- 首个样板为 Python 第一课，助教采用自由输入、推荐问题、确定性知识库和分层提示。
- V1 不登录、不接模型 API，只在浏览器保存提示进度；首版只在样板课出现。

### 已完成

- Python 第一课已重组为七步连续任务，单节课程模板和内容规范同步切换为任务优先结构。
- 建立12张公开知识卡、确定性检索、右下角聊天面板、本地提示层级和无匹配降级。
- 使用宠物生成流程完成原创“小码同学”九行动画精灵图；向左跑安全镜像，失败动作因脱离烟团定向重生成一次，内容与动作已验收通过。
- 24个固定问题Top 1与Top 3均为100%，未知问题降级通过；严格构建和Git差异检查通过。
- 本地预览页、知识库、脚本和1.5MB WebP资源均返回200，助教挂载标记只出现在Python第一课。

### 待真实学习者验证

- 至少 5 名目标用户完成首次输出、核心任务、排错和迁移测试；未达到指标时优先调整任务颗粒度与提示质量。
- 内置浏览器自动控制仍受运行时初始化冲突影响，桌面、移动端、深浅色、键盘和减少动画保留人工视觉复核。

## 2026-07-14：Git 忽略规则与素材目录清理

### 已确认

- `content-inbox/` 和 `source-materials/` 改为完全本地目录，当前版本从 Git 跟踪和公开站点撤下。
- 本地 68MB 素材及加工文件必须保留，不删除实体文件，不重写 Git 历史。
- PDF、压缩包、音视频、Office、大型数据和模型产物全局忽略；课程图片和小型 CSV/JSON/TXT 继续允许入库。

### 本轮实施

- 重组根目录 `.gitignore`，撤下素材目录索引和站点软链接。
- 清理学习地图中的素材清单链接，更新 MkDocs 防御性排除规则。
- 验证本地素材保留、Git 跟踪清空、忽略规则、链接和严格构建。

### 验证结果

- 操作前后 `content-inbox/` 均为 8 个文件，`source-materials/` 均为 3228 个文件/68MB，两份说明文件校验值一致。
- `git ls-files` 不再返回素材目录或站点素材软链接，本地目录进入 ignored 状态。
- 代表性 PDF、压缩包、音视频、Office、数据库、大型数据和模型格式均命中忽略规则；图片、CSV、JSON、TXT 和 Markdown 均可正常入库。
- `mkdocs build --strict` 通过，站点目录与搜索索引不存在素材页面，公开内容不存在指向本地素材清单的链接。

## 2026-07-14：学习路线可读性反馈修订

### 用户反馈

- 可选专业分支聚在小字号列表中，与上方阶段卡的视觉体验不一致。
- 主路线下方重复展开的课程、前置和验收过多，影响快速阅读。
- 从《学习方法》进入工程基础阶段说明后，页面缺少返回第一课的明确操作。

### 本轮处理

- 可选分支改为与主路线一致的可点击阶段卡，保留分支名称、进入条件摘要和“可选”标签。
- 七个主线阶段继续保留锚点，但详细段落压缩为定位简介和必要入口。
- 工程基础阶段说明页增加直接返回《学习方法》的按钮。

### 验证结果

- `mkdocs build --strict` 通过，学习路线、阶段说明和第一课路由均返回 200。
- 学习路线与阶段说明共检查 159 个内部链接、58 个页内锚点，无断链。
- 学习路线保留 7 个阶段简介和 4 个可选分支卡，旧的重复明细标题已全部移出本页。

## 2026-07-14：首页与视觉基线推广

### 已确认

- 学习路线阶段 Stepper 已通过人工评审，确立为项目视觉基线。
- 公开站点收口为首页、开始学习和项目三个顶部入口；项目治理文档只在 GitHub 仓库查看。
- 本轮推广范围锁定为 README 首页、完整课程地图和《学习方法》第一课三类样板。
- 作者公开署名使用“贾飞（@cafelemon）”，首页提供项目仓库和 Star 引导。

### 已完成

- 先同步项目总览、PRD、路线图、架构、验收、决策和学习体验计划。
- 实现三页样板、治理文档构建排除、公开文档链接迁移及统一设计基线。
- README 首页已改为项目简介、适合人群、作者、GitHub 仓库和 Star 引导，并保留三个核心学习入口。
- 完整课程地图已增加三层图例、领域入口和分层正文；《学习方法》已增加课程定位、前置、完成证据和下一课入口。
- `mkdocs build --strict` 通过；生成目录不存在 `site/docs`，搜索索引中不存在 `docs/` 页面，课程与项目 URL 保持不变。
- 自动 UI 规则检查未发现阻断项；状态胶囊和显式焦点轮廓命中属于预期组件规则。

### 待人工视觉复核

- 内置浏览器连接出现运行时初始化冲突，本轮未把源码检查替代为像素验收。
- 保留桌面、平板、手机、深浅色与键盘焦点的人工复核项，收到样板反馈后统一修订。

## 2026-07-15

### 已完成

- 落地Python“容器协议、迭代器与生成器”，完成双主修“容器与迭代”能力配对。
- 课程覆盖Iterable、Iterator、iter/next、StopIteration、生成器状态、单次消费、惰性管道和islice有限消费。
- 双语言阶段作品的Python实现新增惰性标签筛选和进度行生成，一次遍历完成汇总，并在报告边界显式物化输入。
- Python实现通过mypy严格检查和11个unittest，列表、元组与生成器输入输出一致，公开报告继续与C++逐字一致。

### 下一步

- 建设C++“对象、引用、指针、生命周期与RAII”，进入下一组双语言能力对照。
- 保持双语言阶段作品的公开行为契约稳定，在对象与资源课程中按需演进内部结构，不把它扩张为长期项目。
- 可并行启动Python内容分析工具P1.1目录扫描与Markdown/JSON读取，但不提前加入CLI、日志或CI。

## 2026-07-14

### 已完成

- 重组公开站点信息架构：首页分支收纳项目首页、学习路线和完整课程地图，“开始学习”直接进入第一节“学习方法”。
- 将课程阶段恢复为可展开和收起的左侧导航，用更大、加粗的主题色文字区分大章节与具体课名，并把当前页面标题目录恢复到右侧。
- 保持 MkDocs Material 和现有课程 URL 不变；交互式路线与课程地图留作后续渐进增强，本轮未引入 JavaScript 状态或新前端框架。
- 落地Python起步“字符串、列表、字典、集合和元组”正文，重点建立可变性、访问方式、唯一性和结构选择能力。
- 将学习进度报告器升级为多条内存记录，同时使用字符串、列表、字典、集合和元组完成汇总与稳定输出。
- 加入数据结构选择图、共享与复制手动检查点、AI多记录重构审阅、六组微练习和五类验证场景。
- 将新课程接入Python起步课程表、总学习路线和MkDocs导航，未创建练习目录或项目代码。
- 落地Python起步“文件、路径、JSON和简单目录操作”正文，建立工作目录、UTF-8、JSON类型映射和受限扫描能力。
- 将学习进度报告器升级为读取外部JSON并生成独立文本报告，明确输入只读、输出隔离和四层失败定位。
- 新课程已接入Python起步课程表、总学习路线和MkDocs导航，阶段作品继续留在课程正文中。
- 落地Python起步“模块、导入和虚拟环境”正文，覆盖导入方式、主入口保护、模块依赖、命名遮蔽和环境检查。
- 将学习进度报告器拆分为入口、文件读写、分析和报告四个模块，并加入虚拟环境与Git忽略边界。
- 新课程已接入Python起步课程表、总学习路线和MkDocs导航，未安装第三方依赖或创建正式项目代码。
- 落地Python起步收口课程“异常、基本调试和最小自动化测试”，覆盖错误分类、traceback、窄范围捕获、显式校验、breakpoint、assert边界和unittest。
- 将连续五节形成的学习进度报告器沉淀为独立阶段作品，加入输入校验、标准错误、进程退出码和14个标准库测试。
- 阶段作品已覆盖正常、空记录、重复标签、超额完成、UTF-8中文、缺失文件、坏JSON、缺字段、错误类型、非法数值、输入只读和稳定报告。
- Python起步七节课程全部完成；默认路线进入Python/C++双主修，同时解锁Python内容分析工具P1.1并行入口。
- 新增C++核心语言课程入口，确定双主修按类型、接口、函数、容器和工程化等能力主题配对推进，不机械轮流。
- 落地C++首节“从源文件到可执行程序：编译、类型与输入输出”，覆盖构建链路、静态类型、初始化、转换、类型观察和标准流。
- 提供完整学习状态卡、Clang/GCC/MSVC构建命令、AI审阅边界、手动检查点和六组微练习。
- 使用C++20与严格警告实际验证正常和失败输入，并真实复现编译错误与链接错误；本节未引入CMake或新项目目录。
- 新增Python核心语言入口，落地“类型提示、接口与静态检查认知”正文，完成双主修第一组“构建与类型”配对。
- 类型课程覆盖函数与容器标注、可选值、TypeAlias、TypedDict、Sequence、object、Any、cast和JSON运行时边界。
- 在课程正文提供完整类型化报告器副本，不修改Python起步阶段作品，也不新建独立练习或长期项目。
- 从Markdown原样提取代码，在隔离环境使用mypy 2.2.0严格检查零错误，4个unittest和实际报告运行通过。
- 落地C++“函数、声明与程序组织”正文，完成双主修第二组能力配对的C++部分。
- 将学习状态卡按校验、计算、状态判断和输出职责拆分，明确声明、定义、按值参数、只读引用、返回值与副作用边界。
- 加入命名空间、基础重载、四类诊断实验、AI重构审阅和八组微练习，仍保持单文件与标准库边界。
- 从Markdown原样提取完整示例，以C++20严格警告编译，并回归正常和六类边界输入；本轮未创建练习或项目目录。
- 落地Python“可维护函数接口、协议与模块边界”正文，完成双主修第二组“函数与程序组织”配对。
- 课程覆盖位置与关键字参数、可变默认值、能力型输入、返回值、Callable、可调用Protocol、__all__和单向依赖。
- 在正文提供可替换输出的多模块学习报告器，终端函数和测试内存函数无需继承即可满足ReportWriter协议。
- 从Markdown原样提取示例，通过mypy严格检查、6个unittest、真实运行和导入副作用检查；既有Python起步作品保持不变。
- 安装CMake 4.4.0并落地C++“头文件、源文件与最小CMake工程”正文，建立从接口到可重复构建的工程基础。
- 将单文件学习状态卡拆为自包含头文件、业务源文件和入口文件，使用静态库、应用与CTest测试三个目标。
- 课程覆盖include guard、翻译单元、PUBLIC/PRIVATE使用要求、目标级C++20和严格警告，以及配置、构建和测试分层排错。
- 从Markdown原样提取工程，通过CMake配置、零警告构建、CTest和七类输入回归；本轮未创建阶段作品目录。
- 落地C++“STL容器、迭代器与基础算法”，把单条状态卡升级为多条结构化记录。
- 课程覆盖array、vector、map、unordered_map、set、半开区间、容量与失效，以及查找、统计、转换、累计和排序算法。
- 建立双语言学习进度报告器阶段作品，Python与C++使用同一四条记录、业务规则和稳定报告。
- Python版本通过mypy严格检查和7个unittest；C++版本以C++20严格警告构建并通过CTest，两边实际输出一致。
### 下一步

- 建设Python“容器协议、迭代器与生成器”，与C++ STL半开区间和算法形成能力对照。
- 使用双语言阶段作品校准后续Python惰性遍历示例，但不把它扩张为长期项目。
- 可并行启动Python内容分析工具P1.1目录扫描与Markdown/JSON读取，但不提前加入CLI、日志或CI。

## 2026-07-13

### 已完成

- 落地Python起步“函数、参数、返回值和作用域”正文，覆盖函数调用、位置/关键字/默认参数、`return`、`None`、局部作用域和职责拆分。
- 将函数正文作为新版课程内容规范的首个完整样品，加入Mermaid调用流程、可复现代码、AI重构审阅、核心手动检查点和客观验收。
- 启动“学习进度报告器”阶段作品线索，本节只处理一条记录，后续由数据结构和文件课程继续加码，当前不创建独立目录。
- 将函数课程接入Python起步课程表、总学习路线和MkDocs导航，并保持公开标题不显示内部编号。
- 新增课程内容规范，统一AI协作、按类型视觉呈现、可复现代码、核心手动检查点和发布检查要求。
- 建立微练习、连续3-6节阶段作品和跨主线长期项目三级实践体系，明确练习与项目的存放边界。
- 新增单节课程模板，支持概念/理论、编程、算法、Web、AI/LLM和项目六类内容。
- 更新AI Coding工作协议，明确AI优先协作、人工负责审阅判断与验证，完整私人对话不作为公开要求。
- 更新笔记、练习和项目模板，加入AI协作记录、视觉设计、复现信息、手动检查点和回归证据。
- 将课程内容规范接入MkDocs项目文档导航，并保持顶部四个主入口不变。
- 明确新规范从后续新增或扩写内容开始执行，现有课程本轮不批量返工。
- 新增完整课程地图，分离默认必修顺序与全部深化、选修和跨主线依赖。
- 将CS、Web、AI和LLM/Agent统一升级为必修主干、推荐深化和专业选修三层结构。
- 为每个推荐深化和专业选修补充用途、前置、推荐时机、实践、验收、跳过影响、项目关联和素材状态。
- 将算法与机考定位为CS推荐深化验收体系，覆盖分级题型、限时模拟和错题复盘，不单设求职课程或项目。
- 更新学习路线、主线入口、笔记和项目模板，固化三层课程契约。
- 将完整课程地图接入开始学习导航，并保持顶部四个主入口不变。
- 将Python起步的目标从基础语法提升为可独立完成、检查和维护小型程序，并补充模块、异常和最小自动化测试验收。
- 将Python/C++双主修重构为核心语言、工程化、并发网络、运行原理与性能、领域应用五层长期能力线。
- 在CS深化中新增可跳过的工业控制与实时通信专业分支，覆盖PLC、Beckhoff、TwinCAT、ADS、DDS、仿真集成和真实设备联调。
- 明确工业控制分支不阻塞Web、AI或LLM/Agent默认路线，实践顺序采用先软件仿真、后真实硬件。
- 将工业控制数据链路登记为高级项目候选，本轮未创建项目目录或代码。

### 下一步

- 使用本次函数课程的实际结果校准后续课程模板，下一节建设字符串、列表、字典、集合和元组。
- 在首个连续课程组完成时，验证阶段作品是否适合进入 `exercises/` 或既有项目里程碑。
- 按必修主干顺序建设CS正文，先完成数据结构、基础算法、系统、网络和数据库最小核心。
- 在CS必修具备内容后，再建立算法与机考推荐深化的练习结构和验收样例。
- 继续按新的语言深度标准建设Python起步后续正文。
- 建设工业控制课程前，先筛选IEC 61131-3、Beckhoff/TwinCAT、ADS和DDS的官方资料并确认版本与许可边界。

## 2026-06-26

### 已完成

- 完成工程基础阶段0机器验收：9个单元课程契约、相对链接、MkDocs导航、敏感信息边界和线上关键页面检查通过。
- 确认 GitHub Pages 首页、阶段0入口和单元9页面可访问，线上阶段0入口已包含单元1-9和阶段收口检查。
- 完成公开站点信息架构收口：顶部导航改为首页、开始学习、项目、项目文档；页面目录整合到左侧导航。
- 将首页、开始学习、工程基础入门和项目入口改为学习者口径，不再把内部编号、素材库、笔记库和资源库作为公开主入口展示。
- 新增 Python 起步课程入口，并落地变量、基本类型、输入输出正文。
- 落地条件、循环、布尔逻辑正文，继续采用前置、顺序、示例、练习、错误排查、完成标准和下一步结构。
- 将 Python 起步接入开始学习和 MkDocs 导航，公开页面继续使用学习者口径，不展示内部编号。

### 下一步

- 等待真实学习者按阶段0课程顺序完成一次实操验收，收集卡点后再微调正文。
- 继续建设 Python 起步后续课程，优先补齐函数、常用数据结构、文件读取和基本调试。

## 2026-06-25

### 已完成

- 初始化项目文档结构。
- 明确项目定位为公开学习平台与个人学习系统。
- 建立公开内容和私有内容边界。
- 将 `个人学习/` 加入 `.gitignore`。
- 规划首批学习领域：前端、后端、数据库、Python、C++、机器学习、深度学习、强化学习、Agent、LLM。
- 建立第一版公开内容目录：`learning-paths/`、`resources/`、`notes/`、`exercises/`、`projects/`、`templates/`。
- 建立资源、笔记、练习模板。
- 连接 GitHub 远程仓库。
- 推送初始化版本到 `origin/main`。
- 搭建四层结构：学习地图、学习路线、项目实践、资源评测。
- 搭建六条主线：工程基础、编程语言、Web 全栈、CS 核心、AI 基础、LLM/Agent。
- 新增 `maps/`、`reviews/`、`publications/`。
- 扩展路线、主线、项目、评测、个人学习转公开输出模板。
- 新增 `content-inbox/` 素材收纳系统。
- 将《大模型全部学习笔记》2025 登记为 LLM/Agent 待处理素材，访问凭证不入库。
- 新增 `source-materials/` 素材加工区，原始抓取和中间产物只保留本地。
- 已完整拉取《大模型全部学习笔记》2025：2533 个节点、2519 个 Markdown 页面、失败数为 0。
- 完成原素材一级目录体检、项目候选粗筛和广告、过时风险初筛。
- 完成首个 Tool Calling 内容加工样品，建立来源清单、重复矩阵、问题记录、事实核查和内容取舍记录。
- 新增公开笔记 `notes/llm-agent/tool-calling.md`。
- 新增离线可测练习 `exercises/llm-agent/safe-sales-tool-calling/`。
- Tool Calling 练习 10 个离线测试通过，覆盖只读数据库、参数化查询、未知工具、恶意参数和无密钥适配器等场景。
- 完成 2519 页逐项内容审计，未分类页面为 0。
- 生成 Python、机器学习、深度学习、NLP/Transformer、强化学习、LLM、RAG、Agent、部署和多模态覆盖矩阵。
- 明确环境工具、Text2SQL、OpenCV、金融 AI 和 Codex 共 588 页暂缓处理。
- 明确大模型笔记不足以独立组成工程基础、数学、CS、Web和C++课程；相关外部来源已登记，尚未加工到对应阶段。
- 新增 Python、AI 与 LLM 能力依赖图和公开覆盖说明。
- 建立五个跨笔记项目主干：Python 内容分析、结构化数据 ML、文本分类演进、强化学习控制、智能学习助手。
- 扩展笔记、项目、练习和学习路线模板，建立笔记-项目里程碑契约。
- 完成 CS 自学指南和 FastAPI 官方文档的目录级梳理，不下载正文。
- 建立外部来源目录，明确按路线章节和项目里程碑触发最小必要拉取。
- 建立面向小白的唯一默认学习顺序。
- 将工程基础拆为语言前必修与项目中持续深化。
- 补充Docker口径：语言前只做最小认知，复杂容器化和部署在项目中深化。
- 将语言路线调整为Python起步后Python/C++双主修，Java补充，JavaScript/TypeScript在Web阶段进入。
- 将CS最小核心放在Web之前，并明确Web与CS深化并行。
- 明确算法分层：CS学习通用算法，AI学习机器学习、深度学习、NLP/Transformer和强化学习等模型算法。
- 明确数据库实践口径：PostgreSQL/psql优先，MySQL按场景对照，SQLite只用于轻量练习或特定样例。
- 为六条主线增加进入条件、解锁条件和阶段验收入口。
- 将专项素材地图改为来源覆盖说明，不再承担学习顺序职责。
- 将项目主干改为按阶段解锁。
- 新增工程基础阶段0课程入口，并落地单元1学习方法、单元2文件系统正文。
- 落地工程基础阶段0单元3终端与Shell、单元4编辑器正文。
- 落地工程基础阶段0单元5 Markdown、单元6 Git正文。
- 落地工程基础阶段0单元7开发环境、单元8 Docker最小认知正文。
- 落地工程基础阶段0单元9验证习惯正文。
- 完成工程基础阶段0结构和连贯性检查，确认9个单元形成从学习记录到验证闭环的前置课程。
- 新增 MkDocs Material 文档站配置和 GitHub Pages 自动发布 workflow。

### 下一步

- 整理首批免费资源清单。
- 建立资源质量评估标准。
- 从已登记的CS DIY等来源中按需筛选工程基础最小章节。
- 完成工程基础阶段0真实学习者实操验收后，开始建设阶段1 Python起步正文。
- 为机器学习项目选择许可清晰、规模合适的结构化数据集。
- 将 Tool Calling 样品经验用于后续内容加工方法，而不是继续堆独立小项目。
- 建设具体基础课或 API 里程碑时，从外部来源目录按需选择章节。
- 首次推送后检查 GitHub Pages 站点是否成功发布。
