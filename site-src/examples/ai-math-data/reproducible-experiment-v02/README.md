# 可复现实验与评估系统 v0.2

第二版增加标准化的拟合/变换边界：

- 均值和总体标准差只从训练行计算；
- 验证行只能复用训练统计量，不能参与拟合；
- 标准化后训练列均值为 0、总体方差为 1；
- 常量列和形状错配显式拒绝；
- 固定报告比较原始距离与标准化距离。

运行：

```bash
../../../../.venv/bin/python -m unittest -v test_scaling_lab.py
```
