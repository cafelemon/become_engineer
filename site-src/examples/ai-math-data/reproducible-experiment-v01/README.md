# 可复现实验与评估系统 v0.1

第一版用 Python 标准库冻结最小数值契约：

- 向量运算要求长度一致；
- 矩阵必须为矩形，形状固定写作行数 × 列数；
- 矩阵乘向量等于每一行分别做点积；
- 距离只比较相同维度的点；
- 固定报告不含机器耗时或浮点随机噪声。

运行：

```bash
../../../../.venv/bin/python -m unittest -v test_vector_shape_lab.py
```
