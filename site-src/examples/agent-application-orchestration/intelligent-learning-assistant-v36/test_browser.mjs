import{existsSync}from"node:fs";import{chromium}from"../../web-engineering/learning-dashboard-v12/node_modules/playwright/index.mjs";const executablePath=process.env.PLAYWRIGHT_CHROMIUM_EXECUTABLE||"/Users/jiafei/Library/Caches/ms-playwright/chromium_headless_shell-1223/chrome-headless-shell-mac-arm64/chrome-headless-shell";if(!existsSync(executablePath))throw new Error("Chromium missing");const browser=await chromium.launch({headless:true,executablePath});const base=process.env.AGENT_APP_BASE_URL||"http://127.0.0.1:8767";
for(const mode of[{name:"desktop",viewport:{width:1440,height:900},colorScheme:"light",reducedMotion:"no-preference"},{name:"mobile-dark-reduced",viewport:{width:390,height:844},colorScheme:"dark",reducedMotion:"reduce"}]){const context=await browser.newContext(mode);const page=await context.newPage();await page.goto(base,{waitUntil:"networkidle"});await page.locator("#run-form button").click();await page.locator("#run-status").filter({hasText:"waiting_approval"}).waitFor();await page.locator("#approve").click();await page.locator("#run-status").filter({hasText:"approved"}).waitFor();const audit=await page.evaluate(()=>({overflow:document.documentElement.scrollWidth>document.documentElement.clientWidth,steps:document.querySelector("#trace")?.textContent?.includes("steps"),secret:document.body.innerText.includes("Authorization")}));if(audit.overflow||!audit.steps||audit.secret)throw new Error(`${mode.name}:${JSON.stringify(audit)}`);await context.close();}
const nojs=await browser.newContext({viewport:{width:390,height:844},javaScriptEnabled:false,reducedMotion:"reduce"});const page=await nojs.newPage();await page.goto(base,{waitUntil:"domcontentloaded"});if(!(await page.locator("noscript").textContent())?.includes("/docs"))throw new Error("no-js fallback missing");await nojs.close();await browser.close();console.log(JSON.stringify({valid:true,checks:3,approval:true,trace_redacted:true,no_js:true},null,2));
