# 可追踪约束模式实验 v0.1

第一版用有序数组两端指针寻找目标和。每一步输出左右下标、当前和与移动动作；
Python 和 C++20 固定报告逐字一致。

```bash
../../../../.venv/bin/python -m unittest -v test_two_pointer_trace.py
```

正确性依赖输入非递减：当前和过大时，右端对应的其余候选都不可能更小到满足目标；
当前和过小时，左端对应的其余候选都不可能更大到满足目标。
