# 可追踪约束模式实验 v0.8

本版本比较两类结构化动态规划：

- 0/1 背包压缩容量维度时倒序更新，保证每件物品至多使用一次；
- 容量正序的错误实现会把单件物品重复使用；
- 矩阵链按区间长度递增，保证合并 `[left,right]` 前两个子区间已经求解；
- Python 与 C++20 输出同一容量表、反例、最小乘法次数和括号化。

运行：

```bash
../../../../.venv/bin/python -m unittest -v test_structured_dp_trace.py
```
