# 可追踪约束模式实验 v0.5

第五版在有序正整数中枚举每个位置至多使用一次的目标和组合。递归帧遵循
“选择—搜索—撤销”，同层跳过重复值，并在当前和加候选已超过目标时剪掉后缀。

```bash
../../../../.venv/bin/python -m unittest -v test_backtracking_trace.py
```

固定报告记录解、访问节点、被剪候选和搜索结束后的空路径；Python 与 C++20
输出逐字一致。
