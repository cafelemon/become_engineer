# 可追踪约束模式实验 v0.7

本版本用最大不相邻和观察线性动态规划：

- `dp[i]` 固定表示前 `i` 个值的最优和；
- 每步比较跳过当前值与选择当前值；
- 从表尾重建一组确定性最优下标，平局时跳过当前下标；
- 小规模穷举只作为测试 oracle；
- “先拿最大值”反例得到 6，动态规划得到 9。

运行：

```bash
../../../../.venv/bin/python -m unittest -v test_linear_dp_trace.py
```
