# 可诊断系统服务 v0.5

本版本把性能证据分成两层：

- 固定回放样本验证最近秩百分位与 p95 预算，输出可重复。
- `steady_clock` 真实测量本机工作负载，但不对墙钟数值做跨机器断言。

```bash
../../../../.venv/bin/python -m unittest -v test_performance_budget.py
```

真实测量先预热再采样；测试只验证时钟类型、阶段和样本数，不把一次本机运行
当作稳定基准或容量结论。
