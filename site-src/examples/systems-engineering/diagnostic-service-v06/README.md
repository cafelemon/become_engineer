# 可诊断系统服务 v0.6

收口版本执行四组真实本机故障与恢复：

- 暂留一个真实 pipe 描述符，确认仍打开后显式关闭。
- 子进程以 7 退出，父进程保留状态并 `waitpid` 回收。
- 关闭 socket 对端触发真实 `EPIPE`，再创建新连接恢复消息传输。
- 创建临时文件后关闭并删除，确认路径不存在。

```bash
../../../../.venv/bin/python -m unittest -v test_recovery_lab.py
```

测试不访问外网，不扫描或关闭进程不拥有的描述符，也不把打印日志本身当作恢复证据。
